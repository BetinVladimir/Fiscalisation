# MVP implementation status

## Implemented

- Fiscal public HTTP vertical: versioning, idempotency, sale, first-line УНП, EUR/tax validation, cash/card, reversal, readiness, shifts, cash movement, X/Z/KLEN/FM report operations, connectivity, BLE session resource and edge sync ACK.
- Fiscal polling surface includes tenant-filtered operation listing, sale cancellation, normalized receipt retrieval and explicit UNKNOWN→RECONCILING transition.
- Fiscal administrative surface now persists tenant-isolated organizations, locations, registers, operators and devices with UUIDs, optimistic versions, uniqueness/reference validation, device bindings, capability manifests, provisioning sessions and redacted diagnostics.
- Device activation now fails closed from creation onward: every new device must start in `DRAFT`, cannot skip `PENDING_SERVICE_ACTIVATION`, only ACTIVE devices may bind to an ACTIVE register, ACTIVE devices cannot request a bootstrap provisioning session, and an ACTIVE PROD device requires both approved-type and service-contract evidence references. These controls validate evidence presence without claiming the external evidence is genuine.
- Created provisioning sessions are now durable tenant-bound resources, survive repository and real PostgreSQL typed-only restart and participate in the normal audit/persistence path. The storage-mode-2 integration test proves their exact payload/tenant/state projection and reconstruction. Cross-tenant reads fail closed; non-provisionable device state returns explicit HTTP 409 `DEVICE_NOT_PROVISIONABLE`, while an absent/foreign device remains 404.
- Shifts are tenant-scoped and support list/get/open/close. Reports have persistent immutable JSON artifacts and list/get/download endpoints; resource and fiscal mutations append a persistent hash-chained audit ledger exposed read-only.
- Compliance exports persist real tenant-filtered JSON, CSV or minimal standards-compliant XLSX artifacts with SHA-256 manifests; invalid periods/types/formats and cross-tenant reads are rejected.
- Public API enforces the OpenAPI 16–255 character idempotency-key constraint; receipts include operation, total, issue time and immutable artifact identifiers.
- Effective-dated BG policy endpoints (`/country-policy`, `/tax-groups?effective_at=`) now expose the EUR policy and reject dates without an active policy. Sale lines are checked against the reviewed policy catalog; only the evidenced MVP B/20.00 seed is executable, while unreviewed A/C–H mappings fail closed pending a signed policy update.
- Fiscal list surfaces now implement the contract cursor/limit bounds with opaque cursors and `PageMeta`; operations support state/register filters, reports support register filtering, and audit events support actor/УНП/period filtering with strict RFC3339 period validation.
- MiniPOS product, employee and order collections now implement their OpenAPI `cursor`/`limit` contract with the same 1–200 bound, opaque cursors, explicit invalid-cursor rejection and deterministic ID ordering in both autonomous PostgreSQL and in-memory DEV modes. Direct API/race coverage proves multi-page traversal without duplicates and fail-closed malformed pagination. Shift recovery remains deliberately filter-only because its OpenAPI operation does not declare pagination.
- Fiscal resource, operation, shift, report and audit collections now have a deterministic `(created/occurred timestamp, ID)` total order before opaque offset pagination. This closes the map/storage-order gap that could otherwise duplicate or skip records across page requests; a public operations API/race test exercises equal-timestamp tie-breaking and complete two-page traversal.
- Deterministic non-production simulator with hard PROD configuration guard.
- MiniPOS autonomous domain/API: employees/products with optimistic updates, shifts, orders, lines, checkout and reports; Fiscal is accessed only through its public HTTP API.
- MiniPOS now persists and exposes its autonomous single-location/single-workstation configuration with optimistic versioning. BeeMiniPOS edits location name/address, workstation name and the bound Fiscal register through the MiniPOS public API; changing the binding invalidates the active local BLE session in the UI.
- Fiscal and autonomous MiniPOS now persist every repository collection entry as an independent PostgreSQL JSONB row in one serializable transaction, each with its own authoritative generation marker and automatic one-time migration from the former aggregate snapshot. Empty authoritative state cannot resurrect legacy data; sales/orders, outbox/inbox, idempotency and sequence state survive restart in their isolated databases.
- Edge authority lease/fencing allocation, durable hash-chained SQLite WAL journal, three-month ACK-gated GC, encrypted directional BLE sessions and durable-before-device runtime.
- Edge sync now builds the full OpenAPI batch from unacknowledged SQLite events (maximum 100): normalized device events, contiguous journal sequence, previous committed hash, per-event chain, batch SHA-256 and HMAC signature. Fiscal accepts JSON and CBOR, verifies every layer before durable commit, and returns the complete signed acknowledgement with committed event hash, operation results and rejected array. Before the first network side effect, the uploader persists the exact pending first/last sequence and batch SHA-256 in SQLite. Ambiguous response loss, newly appended events and process restart therefore resend the identical idempotency key/body instead of expanding the batch and duplicating a backend commit. A valid bound ACK atomically advances cursor, acknowledges journal rows and clears the pending marker. A shared deterministic golden vector proves Edge builder/Fiscal verifier compatibility.
- An executable accelerated soak gate models all 864 five-minute slots across 72 hours of clean outages/ambiguous-after-commit losses and all 1008 ten-minute slots across seven journal days. It reopens SQLite daily, verifies zero lost events and duplicate backend commits, exact ACK replay, final cursor/hash, chain integrity, three-month ACK-gated retention, purge anchor continuity and produces a machine-readable Go JSON report. This is software time-model evidence only and does not promote physical SD/HIL endurance.
- Accepted offline `FISCAL_SALE` commands are persisted by Fiscal and may be completed by a later sync batch after either side restarts. A terminal device result atomically materializes the tenant sale, operation, normalized receipt artifact and webhook/audit outbox event; malformed currency, totals, operator, tax, payment or tenant/device bindings fail closed. Same-batch acceptance/completion and split-batch correlation are idempotent.
- Signed Fiscal webhook outbox with retry/backoff and MiniPOS HMAC receiver; polling remains available as the authoritative fallback.
- Fiscal→MiniPOS/Edge webhook delivery now follows the canonical `t,kid,v1` signature over `timestamp.raw_body`, and both receivers enforce constant-time HMAC plus the 300-second replay window before JSON parsing. Delivery failures are isolated per endpoint, use the documented 10s/30s/2m/10m/1h/6h/24h schedule, and HTTP 410 durably disables a dynamic endpoint instead of retrying it.
- Dynamic tenant-scoped webhook endpoint create/get/update/disable/secret-rotation lifecycle is persistent and idempotent. Secrets are disclosed only once, GET is redacted, a 24-hour previous-secret overlap is recorded, URL/event validation fails closed, and the dispatcher selects active endpoints by tenant/event and signs with each endpoint secret. Per-endpoint delivery checkpoints prevent replaying already successful destinations when another destination retries; the egress client disables proxies/redirects and rejects private/loopback/link-local DNS answers at connect time.
- MiniPOS checkout validates malformed Fiscal responses without panic and persists tenant-scoped idempotency request hashes; a reused key with a different payload is rejected.
- Every public MiniPOS mutation now has a durable tenant/path/method scoped idempotency record with request hash, exact response replay, restart recovery and mismatch rejection.
- MiniPOS products/employees/shifts/orders now emit UUID/resource metadata, status/unit/roles, exact EUR totals and timestamps required by OpenAPI; line writes enforce optimistic `If-Match`, UUID, quantity/tax/money and same-tenant product ownership.
- Signed Fiscal events are first durably stored with their raw body in the autonomous MiniPOS webhook inbox, then applied by monotonic fiscal version; event replay and event-ID payload substitution are handled safely across restart.
- Offline Edge sale webhooks carry `external_id`; MiniPOS links them to the locally created order even before a cloud sale ID exists, persists the Fiscal sale/operation IDs and applies monotonic terminal state. This closes eventual reconciliation of a successful BLE checkout into the autonomous MiniPOS database.
- BLE session issue/refresh/revoke is durable, tenant-, app-, operator- and issuing-OIDC-subject-scoped, signed and exposes the canonical OpenAPI field names and GATT UUIDs. MiniPOS prepares an X25519 key before REST issuance; Fiscal signs its public key into the ticket, and Edge accepts HELLO only when the presented ephemeral key matches, turning the ticket into proof-of-possession authority instead of a transferable bearer artifact. Every established gateway channel rechecks ticket expiry and the durable revocation journal before decrypting each frame. MiniPOS clears READY, cancels waiters, disconnects and allows a new explicit issuance after expiry/connect failure/logout/configuration change; an issued but failed connection is revoked best-effort through the public API. Issue fails closed unless the requested register belongs to the tenant, is ACTIVE and is bound to an ACTIVE tenant-owned fiscal/smart device; refresh re-evaluates that binding and operator authority, rotates the nonce and monotonically advances the fencing token. A different authenticated subject cannot refresh or revoke the lease, legacy unbound rows fail closed, and revoke/replay returns the canonical empty `204`.
- Registry authority is now enforced at every central execution boundary: authenticated sale creation, shift opening, payment/fiscal sale, reversal, cash movement and report commands require the tenant-owned ACTIVE register plus its ACTIVE fiscal/smart-device binding. The check runs before driver invocation. Reversal and generic report/cash operations now execute through the driver and persist `FISCALIZED`, `FAILED` or `UNKNOWN/RECONCILE` instead of manufacturing a direct success reference in the service layer.
- Reversal policy now fails closed on the documented reason set. `OPERATOR_ERROR` uses the Europe/Sofia civil calendar and is rejected from 00:00 on day 8 of the month following the original fiscal operation, implementing Art. 31(4); customer return, complaint and tax-base reduction remain event-bound to the API invocation. Unknown reasons, future original timestamps and expired operator errors are rejected before driver execution.
- Authenticated sale creation and shift opening additionally require an operator code registered in the same tenant whose `active_from`/`active_to` interval contains the current instant. Missing, cross-tenant, not-yet-active and expired operators fail closed. Fiscal operator authority remains separate from the autonomous MiniPOS employee record and is provisioned through the Fiscal public API.
- BLE Edge primitives now include canonical RFC 8949 deterministic CBOR, strict decoding, payload hashing, X25519/HKDF-SHA-256 key agreement, AES-256-GCM directional counters, exact binary GATT framing and MTU 185/247/517 chunk tests.
- BLE flow control now enforces one in-flight message, ACK state (`highest_contiguous`/missing bitmap), duplicate consistency, BUSY, cancel and resume; an integration test carries a canonical encrypted/chunked CBOR sale through reassembly into the durable Edge runtime and proves device exactly-once replay.
- Edge now implements the complete ticket-validated handshake state machine: HELLO binding/scope/expiry checks, X25519/HKDF-SHA-256, AES-256-GCM encrypted AUTH_PROOF, constant-time proof validation, READY and replay rejection, producing the directional encrypted transport session only after success.
- MiniPOS has an executable REST/BLE/BLOCK decision model with separate transport/device metrics and tests for cloud loss, final-device loss and session expiry. Chrome Web Bluetooth performs secure-context/user-gesture discovery and the matching HELLO→CHALLENGE→AUTH_PROOF→READY WebCrypto handshake; fiscal execution remains unavailable until READY. After READY it canonical-CBOR encodes commands, splits them for the negotiated ATT MTU and emits the exact encrypted BLE v1 binary frames with directional key derivation and monotonic counters. Incoming directional frames enforce tag/counter/header checks and are gap-aware reassembled with one-message BUSY and duplicate-consistency rules before reaching the UI; encrypted ACK/NACK progress is returned through the Flow characteristic.
- MiniPOS checkout transport now freezes the route before the first side effect: healthy cloud uses REST, unavailable cloud uses BLE only with a live final device and valid READY session, and final-device loss blocks the sale. An ambiguous REST result becomes `UNKNOWN/RECONCILE` and is never retried over BLE. Offline commands carry a bounded expiry and SHA-256 of canonical CBOR payload.
- BeeMiniPOS now has a native Android/iOS GATT client based on `react-native-ble-plx`: platform permission handling, service-filtered scan, connect/discovery, MTU negotiation, notifications, authenticated HELLO→CHALLENGE→AUTH_PROOF→READY, encrypted command chunks, ACK/NACK flow and correlated terminal result. Portable Noble cryptography removes WebCrypto/base64 browser dependencies while preserving the same X25519/HKDF/AES-GCM protocol on Web and native.
- Web BLE now offers a correlated send-and-wait primitive with bounded timeout, exact message/operation binding, duplicate suppression and waiter cleanup. The reusable fiscal checkout adapter maps both REST and BLE terminal results into the same COMPLETED/ACCEPTED/UNKNOWN/BLOCKED model. A short-lived encrypted `DEVICE_PROBE` traverses MiniPOS→Edge→final ФУ without allocating УНП/operation sequence; only its fresh READY result may authorize local fallback.
- BLE revoke is atomically persisted with a `ble.session.revoked` outbox event. Edge has a restart-durable SQLite revocation cache with expiry GC, and its handshake rejects cached revoked tickets before emitting CHALLENGE.
- Edge is now an executable, container-buildable process with guarded DEV/PROD configuration, graceful shutdown, periodic bounded sync draining, health endpoint and an HMAC-authenticated Fiscal control webhook. BLE revocation is acknowledged only after durable SQLite commit and survives process restart; invalid/tampered/non-revocation events fail closed.
- The Edge executable now composes authority, journal, runtime and a configured final-device adapter. A bearer-protected DEV/HIL transport executes canonical commands through the real durable path, enforces tenant/register/device/fencing binding, exposes the independent final-device probe, and is absent in PROD. Process restart smoke proves a completed command retains the same sequences/result with exactly two journal records and no second device execution. `simulator` hard-fails in PROD; unapproved tracks use an always-unreachable `unsupported` adapter.
- Edge has a transport-neutral GATT command processor that decrypts/reassembles frames, strictly validates CBOR envelope/hash/expiry, invokes the durable runtime and returns encrypted result frames correlated by message ID. Nested CBOR maps are normalized to string-keyed objects before driver JSON serialization; an encrypted end-to-end test covers the complete path and proves expired commands never execute.
- The GATT processor binds every command and probe to the authenticated ticket tenant/register/edge/fencing tuple before runtime invocation; cross-tenant or stale-fence commands never reach the device. BLE session `binding_version` exposes the actual fencing token through the existing OpenAPI field.
- Connectivity probes are durable and tenant-isolated; readiness/probe returns `BLOCK` when the final fiscal device is unavailable. Simulator unknown-after-send creates `UNKNOWN` and cannot be blindly retried.
- Readiness is no longer driver/simulator-only: the public device endpoint first requires the requested device to belong to the authenticated tenant, be `ACTIVE` and have fiscal capability. Connectivity probes additionally require an ACTIVE register with an ACTIVE bound fiscal device before probing the driver. Unknown, foreign, DRAFT, BLOCKED and unbound devices return `BLOCK` even when the simulator is healthy.
- Core sale/operation JSON names, sale states, payment IDs and required action arrays are aligned with the canonical OpenAPI contract.
- Authenticated tenant-scoped Fiscal sales/operations and MiniPOS catalog/order filtering. Protected endpoints require the OpenAPI OAuth authorization scope `fiscal.base`. Public APIs enforce a centralized fail-closed RBAC policy: cashier sale/shift access, supervisor fiscal reports/reversal/export, administrator configuration/provisioning/webhooks, auditor read-only access and service-only Edge sync/diagnostics. Missing scopes and unknown roles are rejected.
- Production authentication now requires OIDC RS256/JWKS and rejects configured HMAC, missing issuer/audience/JWKS or non-HTTPS trust endpoints. Verification pins `alg=RS256`, selects a non-empty `kid`, validates issuer/audience/expiry/tenant/roles/scope, bounds JWKS downloads, caches keys and refreshes immediately for a rotated unknown `kid`; tests cover rotation, multiple audiences, wrong issuer/audience and attacker signatures. HS256 remains DEV/E2E-only.
- Production MiniPOS→Fiscal authentication now uses OAuth 2.0 client credentials over HTTPS instead of a static service token. The provider uses HTTP Basic client authentication, explicit scope/audience, validates Bearer token responses and bounded expiry, single-flights concurrent acquisition, caches with a 30-second safety window, invalidates on 401 and retries the same idempotent Fiscal request exactly once. Static `FISCAL_AUTH_TOKEN` is DEV-only and rejected in PROD.
- Both public APIs enforce an in-process tenant-keyed (authenticated) or source-IP-keyed (DEV) fixed-window limit, return HTTP 429 plus `Retry-After`, and leave health and the separately HMAC-authenticated internal webhook path outside that public-client budget.
- Typed Fiscal and MiniPOS schemas define LOGIN non-owner/no-`BYPASSRLS` roles, fail-closed transaction-local tenant context functions and `FORCE ROW LEVEL SECURITY` policies, including ownership-through-parent policies for line/event/order descendants. Each backend opens a physically separate non-owner reader pool; PROD rejects missing RLS credentials or reuse of the writer database user. Real PostgreSQL tests authenticate with those reader credentials and prove foreign-tenant invisibility and rejection of cross-tenant inserts.
- Fiscal sales/operations/shifts/resources plus tenant-bound UNP sequence, API replay, webhook outbox, BLE sessions, connectivity probes, Edge pending commands, immutable binary artifacts with SHA-256, Edge sync acknowledgements and audit events, and MiniPOS products/employees/shifts/orders/single-site configuration/API replay/webhook inbox/checkout result+hash checkpoints have authoritative typed projections with explicit tenant keys, domain constraints, indexes and FORCE RLS. UNP allocation uses a tenant+register key and atomically absorbs a pre-tenant register sequence on its first authenticated allocation without reusing a number; tenantless allocation remains only in the explicit in-memory DEV fallback. Authenticated Edge sync keys its journal cursor by tenant+edge, rejects a command whose embedded tenant differs from the JWT tenant and persists its acknowledgement in the same transaction as materialized sale/operation/artifact/outbox/pending changes. Artifact keys are tenant+id; pre-tenant artifacts migrate on first authenticated read and roll back to the legacy key if persistence fails. Checkout result and its 64-character request hash are projected in the same serializable repository transaction; malformed hashes roll back both representations. Every projection upsert/delete assumes the non-owner role and transaction-local tenant context inside the same serializable transaction as compatibility state. Integration tests prove normalization, foreign-tenant invisibility, cross-tenant reassignment rejection and atomic rollback of both representations. MiniPOS requires webhook `tenant_id`, persists it in the inbox and refuses to link an event to a foreign-tenant order. Legacy pre-typed MiniPOS configuration rows remain compatibility-readable and are normalized on the next API save.
- Persistent single-entity and collection hot-path reads for Fiscal sale/operation/shift/admin resources and MiniPOS product/employee/shift/order execute against typed tables through the separate reader identity in read-only transactions with transaction-local tenant/organization context. RLS returns no row for a foreign tenant; real PostgreSQL tests cover allowed and denied reads. DEV without persistent storage retains an explicit in-memory fallback.
- Fiscal authenticated orchestration now also reads BLE sessions, connectivity probes, Edge pending commands, Edge journal acknowledgements, replay records, immutable artifacts and audit events from their typed RLS tables. The cross-tenant outbox dispatcher uses a separate explicitly system-only typed reader through the writer identity; that reader is not exposed to public handlers. Sale/payment/cancel/reversal/reconciliation/receipt/shift mutation endpoints perform their authoritative precondition read with the request tenant rather than re-reading an unscoped compatibility map. Tests prove a foreign tenant cannot fall back to a stale in-memory sale.
- MiniPOS API replay and checkout result/hash checkpoints are read from typed RLS tables. A repeated checkout resolves the typed checkpoint and validates the original request hash before any Fiscal call; a half-written/invalid checkpoint or reader failure fails closed. Product/employee/order/shift mutations validate the tenant-owned typed version/state before changing the compatibility coordinator, and sales reporting reads typed tenant orders. Real PostgreSQL tests cover permitted and foreign replay/webhook/checkout reads.
- Both PostgreSQL row stores now expose an atomic repeatable-read snapshot plus monotonic generation token and compare-and-swap save. Two backend instances loaded at the same generation cannot overwrite one another: the stale writer receives a generation conflict before any compatibility or typed row changes, then reloads the authoritative snapshot/generation into its in-process coordinator. Unit/race tests prove failed stale mutations disappear from memory; real PostgreSQL tests use two independent connections and prove the first committed state survives unchanged. This closes lost-update risk while aggregate-specific write commands are introduced incrementally.
- Runtime persistence now keeps the last confirmed snapshot only as a delta baseline. Each successful mutation sends explicit changed/deleted state rows to `SaveDeltaVersioned`; PostgreSQL locks/checks generation first, then upserts/deletes only those rows and their typed projections. It no longer queries all compatibility rows or derives database-wide stale deletes on the normal write path. Real PostgreSQL tests inject an unrelated row absent from the coordinator baseline and prove a delta mutation preserves it, while coordinator tests assert the delta interface—not full `SaveVersioned`—is used.
- The PostgreSQL migration path now has an executable legacy-to-typed drill for both products: a real legacy `runtime_snapshots` payload is migrated atomically, then the test verifies both the reconstructed row state and its tenant-bound authoritative typed sale/product projection. Compatibility removal is still gated on a typed-only restart and rollback drill.
- Both products now activate typed-only durable mode only after an exact-payload coverage guard succeeds. Main constrained aggregates retain their exact domain payload alongside indexed columns; technical aggregates are reconstructed from payload or a canonical typed value. Mode 2 stops reading and writing compatibility rows, preserves MiniPOS sequence in versioned meta and rebuilds the coordinator cache solely from typed tables. PostgreSQL tests corrupt the dormant compatibility copy and prove restart still returns typed state; typed constraint failures roll back both data and generation. Physical deletion remains a separately approved destructive migration after the rollback window.
- Stage 21 fault regression is now an executable release gate with 20 cases across device, BLE, authority, restart, sync, storage, retention, clock and routing. New tests prove partial/signed-wrong ACK, backend rollback, malformed ACK and transport loss never acknowledge data and that the identical idempotent batch recovers; authority handback requires strictly increasing fencing/ranges; storage 70/85 remains operational while 95/100 blocks; clock rollback cannot shorten retention.
- Stage 23 security regression is gated by 26 executable cases across authentication, authorization, webhook, SSRF, replay, revoke, rate limiting, idempotency, production guards, secret rotation and OTA. A discovered weakness accepting a raw JWT without the `Bearer` scheme was fixed in both backends; MiniPOS webhook auth exemption is now exact-path only. MiniPOS also proves ADMIN-only immutable OIDC employee binding, cross-employee/app-instance denial, durable logout revocation and restart fail-closed behavior. Sensitive payment-data scanning remains clean.
- Edge now has a durable hardware-independent signed OTA control plane: Ed25519 manifests bind model/hardware, monotonic counter, minimum bootloader, rollout ring, expiry, SHA-256/size, SBOM, fiscal protocol and country pack. Verification occurs before staging and again before boot. SQLite persists A/B pending/health state across restart with optimistic revision/CAS so a stale supervisor cannot overwrite a newer decision; downgrade/replay fail closed, health timeout rolls back only above the signed vulnerability floor, and an unsafe previous image yields `RECOVERY_REQUIRED`. Secure boot, physical flashing/recovery partition and vendor compatibility remain external HIL—not simulator PASS.
- BG software evidence was strengthened: totals now run a 120-case EUR quantity/price/split property matrix; UNP runs a 128-way concurrent uniqueness/monotonicity/tenant-scope test; reversal persists immutable reason and validated original fiscal reference and rejects a second reversal; three-month retention explicitly covers clock rollback and contiguous safe GC. Vendor/SD/legal portions remain external gates rather than simulated PASS.
- Separate Fiscal and MiniPOS PostgreSQL migration baselines.
- Two isolated Compose projects with distinct PostgreSQL, private/ingress networks, Caddy ingress and explicit DEV/PROD overlays.
- Both backends tolerate transient Docker DNS/database readiness races with a bounded 30-second startup retry; permanent errors still terminate startup. DEV Caddy listens on `:80` so host and cross-compose routes work, while PROD remains bound to the explicitly required HTTPS site name.
- Touch MiniPOS and fiscal administration Expo UI for Android/iOS/Web. BeeFiscalApp now loads registered devices, filtered operations, persisted fiscal reports, effective country/tax policy and readiness/connectivity data from the public API; device and report rows are no longer hard-coded or locally simulated.
- BeeMiniPOS now exposes the required cross-platform IDs for transport, BLE, cash/card payment, unknown outcome and reconciliation. Interactive controls meet the 48dp minimum and primary payment/reconciliation CTAs meet 56dp. Once fiscal routing can have side effects, the UI freezes the order, disables both payment methods, displays a prominent no-retry reconciliation panel and performs only `GET /orders/{id}` polling; it clears the gate only for terminal `COMPLETED`, `FAILED` or `CANCELLED` state and never invokes checkout from reconciliation.
- MiniPOS order responses now derive `allowed_actions` from authoritative backend state (`ADD_LINE`, `CHECKOUT`, `READ`, `RECEIPT`) and expose the completion schema in locked runtime OpenAPI. After an ambiguous checkout, Expo performs one authoritative GET before rendering recovery and offers GET-only polling only when the returned order includes `READ`; missing permission fails closed. BeeFiscalApp likewise renders/submits operation reconciliation only when Fiscal `allowed_actions` contains `RECONCILE`. Browser E2E proves both the permitted action and the absent-action negative path and counts exactly one reconciliation submission.
- Stage 22 now includes a machine-checked 18-case Android/iOS/Web UI acceptance matrix for both Expo apps plus a real Playwright Web interaction gate. MiniPOS executes blocked-without-shift, configuration/product/employee editors, persisted unique barcode creation, exact scanner/keyboard barcode lookup, report, shift, quantity, successful cash checkout, ambiguous card checkout, GET-only reconciliation and final Z-linked shift close while asserting that reconciliation never repeats payment. A separate PROD export proves the static-token environment variable is ignored and sales remain hidden until OIDC Authorization Code + PKCE can establish a bound operator session. FiscalApp executes hop-by-hop readiness, UNKNOWN rendering, server-driven `allowed_actions` reconciliation (including fail-closed absence), X-report creation, administration persistence and country-policy navigation. The runner exports both apps against isolated public-API mocks and is part of `make regression`; native interaction, physical BLE and screen-reader HIL remain external gates.
- Authenticated MiniPOS shift closure is now a durable `OPEN → CLOSING → CLOSED|BLOCKED_RECONCILIATION` state machine. It first rejects unresolved orders, then requests exactly one idempotent Fiscal `Z` report through the public API, persists the operation/fiscal references and closes only for a final `FISCALIZED` response. Timeout, non-final response or missing reference leaves the shift blocked, prevents a replacement shift on the register and requires reconciliation; Expo refuses to display success without both references. PostgreSQL typed projection and the two-Compose E2E preserve and verify this linkage.
- BeeFiscalApp parity now includes accessible tab semantics/selection, live Core and transport status, labelled device selection, UNKNOWN operation marker, report actions and end-to-end readiness probe. Its navigation/input/action targets are at least 48dp and the primary probe is 56dp; the same executable UI matrix prevents regression of these anchors.
- BeeFiscalApp now includes a public-API-backed administration workspace for locations, registers, Fiscal operators, DEV fiscal devices and register-to-device bindings. Existing resources are selectable and editable with optimistic `If-Match` versions; new resources use POST, bindings use the canonical endpoint, and success/error/loading/empty states are announced through accessible UI. No editor writes local authoritative records.
- Both Expo UIs now call backend APIs; MiniPOS no longer simulates a successful receipt and includes product (including optional tenant-unique barcode), employee, location/workstation editors, active-employee selection, exact barcode/SKU scanner lookup, quantity controls and its commercial sales report. Barcode is preserved through the canonical OpenAPI field, domain validation, autonomous PostgreSQL typed projection and browser interaction test.
- MiniPOS commercial reporting now implements the canonical required RFC3339 period instead of returning a legacy counter object. The backend validates strict `from < to`, applies half-open `[from,to)` order selection, computes exact integer-cent net gross, subtracts append-only reversals and returns deterministic CASH/CARD payment totals. Checkout persists only non-sensitive tender type/amount evidence in the autonomous order payload and typed `payments` JSONB projection; tenant reads, restart and database backup/restore preserve it. The touch UI always supplies a 30-day ordered period, and direct API/domain/browser/two-Compose tests cover invalid/equal periods, exact boundary exclusion, split tender and reversal netting.
- Persisted tender evidence is explicitly internal and is removed from both individual and paginated public `Order` representations before OpenAPI response validation. Only the documented sales-report `payments[]` aggregate is public. A direct serialization test proves storage retains the evidence while Order/list responses cannot expose it, preventing an undocumented API surface from appearing merely because the same domain object is durable.
- Fiscal `GET /shifts` now applies its documented `register_id` filter before pagination; a public handler test proves other-register shifts are excluded.
- BeeMiniPOS and BeeFiscalApp now consume every cursor page instead of silently truncating paginated lists at the first 50/100 records. Products, authorized employees, reversible orders, devices, operations, reports, audit events and administration collections use a bounded 100-page traversal that preserves filters and fails closed on missing or repeated cursors. Shift recovery continues to use its documented non-paginated filtered operation. Unit tests cover multi-page aggregation and malformed sequences; UI acceptance now contains 38 cases.
- BeeFiscalApp now closes the missing Stage 17 audit-screen requirement. Only ADMIN/AUDITOR roles receive the `Одит` tab; it reads the immutable `audit-events` public OpenAPI collection and renders actor, action, object, timestamp, UNP and event hash. The browser journey proves a real audit record is displayed before continuing to ADMIN-only editors.
- BeeFiscalApp no longer invents the non-UUID register `FD000001`. With no configured register, operations/reports are requested without a filter; every configured register filter and every register-resource mutation path (probe, report, BLE session, update and binding) must be an OpenAPI UUID before request construction. Invalid report/BLE/binding controls fail closed. Unit tests cover omission, valid UUID and legacy-invalid rejection, and the browser journey asserts all emitted collection filters are UUIDs.
- Kotlin stable driver interface and a buildable Daisy SMART S Android STUB app with deterministic fiscal/card/barcode/error scenarios, activated-terminal enforcement, debug-only plus PROD constructor guards and contract tests. Debug and unsigned release APKs compile; the release artifact cannot instantiate the STUB because `BuildConfig.DEBUG` is false.
- The C++ IoT abstraction classifies all 88 Daisy and 73 Datecs commands and implements validated vendor payload builders/golden response parsers for every `Supported` command and every optional command in both vendors. Daisy has 45 core plus all 16 `Optional` semantics, including display/printer/drawer, invoice customer data, barcode types 1–13, customer QR templates, display configuration, 2000-record customer-directory operations and Compact-family battery telemetry. Datecs has 40 core plus all 12 `Optional` commands. Conditional/model restrictions remain evidence, not activation: Daisy 173 is only for Compact M/S/FX21, and all optional dispositions remain unchanged. Command 135 remains `GetDiagnosticInfo`; regulatory delivery state comes only from 71. Privileged/excluded commands, HIL and unverified CSV-column meaning are not promoted implicitly.
- C++ protocol abstraction and exhaustive safe-fail command registries for Daisy and Datecs. Every one of the 88/73 documented IDs produces a disposition/retry/canonical classification; unknown IDs and conditional payment commands fail closed.
- Portable Daisy/Datecs frame encoders/parsers with BCC validation and compiled inventories of all 88 Daisy / 73 Datecs documented command codes.
- Edge measures allocated SQLite pages against `EDGE_STORAGE_QUOTA_BYTES`, exposes authenticated DEV/HIL storage telemetry, classifies 70/85/95/100% as WARNING/HIGH/CRITICAL/FULL and blocks a new fiscal command at 95% before probe, sequence allocation, journal append or device execution. Existing durable results remain replayable.
- A machine-checked release evidence generator inventories Go, npm and container dependencies as CycloneDX 1.6, emits in-toto Statement v1/SLSA Provenance v1 bound to the exact SBOM/source commit, hashes contracts/migrations/deployment baselines and emits a release manifest/checksum set. `RELEASE_CREATED_AT` makes the scan-then-package flow byte reproducible. Unsigned generation remains `UNSIGNED_NO_GO`. An optional Ed25519 path signs the exact manifest bytes, transitively binds provenance and an optional normalized vulnerability report, pins the public-key SHA-256 and requires an independently supplied trusted public key. The verifier rejects packaged-key substitution, unsafe/incomplete inventory, byte tampering, stale/incomplete or foreign-SBOM scans, inconsistent counts and any Critical/High finding. A valid signature and scan still remain `PROD_NO_GO` until external gates close.

## Evidence currently passing

- Go unit/API/integration/race tests for three Go modules.
- Both row-stores have opt-in real PostgreSQL integration suites covering multi-entity rows, close/reopen recovery, legacy snapshot migration and the authoritative-empty-state corner case.
- TypeScript checks for both Expo apps.
- All four DEV/PROD Compose configurations render with required variables.
- Manual local E2E MiniPOS→Fiscal simulator reached `COMPLETED` with fiscal sale/operation references.
- Clean-room container smokes proved MiniPOS and Fiscal data survive backend restart through Caddy/PostgreSQL; the temporary containers/volumes were removed afterwards.
- Automated two-project Compose E2E passes through both Caddy entry points using a short-lived DEV/E2E HS256 bearer session with an explicit tenant, `ADMIN` role and `fiscal.base` scope; the same authenticated tenant token is used by MiniPOS for its public Fiscal API calls, so FORCE RLS is exercised with real claims rather than an empty DEV tenant. The scenario covers autonomous configuration/catalog/employee/shift/order creation, MiniPOS→Fiscal EUR cash fiscalization, completed-state verification, both backend restarts with recovery, and MiniPOS database autonomy while Fiscal PostgreSQL is stopped. Its fault branch stops Fiscal before checkout, requires HTTP 502 plus a durably persisted `UNKNOWN` order, then proves replay of the same idempotency key after recovery remains the same non-success response and does not silently retry the fiscal sale. The test also creates custom-format backups of both isolated PostgreSQL databases, restores them to fresh databases, checks restored state rows, and enforces an MVP RTO below 120 seconds. HTTP assertions run from pinned `curlimages/curl:8.12.1` through a disposable test-only ingress containing both Caddy containers, both backends and the test client; every curl has bounded connect/total time, including the health loop, so a Docker network blackhole cannot bypass the harness timeout. Neither PostgreSQL is attached to ingress. The E2E harness discovers container IPs and injects the supported `*_DB_HOST`/`*_UPSTREAM` overrides to avoid reproducible Docker Desktop embedded-DNS and dynamic-network-routing defects; normal DEV/PROD defaults remain Compose service names and backend isolation. MiniPOS still invokes Fiscal only through Fiscal Caddy/public API, and Caddy remains each deployed system's sole external entry point. The script removes only its PID-scoped projects/network/volumes and prints response bodies plus step-scoped logs on failure.
- A clean isolated rerun on 2026-08-09 passed the complete two-Compose scenario after an earlier Docker Desktop bridge startup failure; the passing rerun used fresh PID-scoped networks/volumes and required no application or harness retry that could conceal a deterministic service failure.
- The two-Compose harness now provisions location, register, DEV fiscal device through the mandatory `DRAFT → PENDING_SERVICE_ACTIVATION → ACTIVE` transitions, binding and active Fiscal operator through the authenticated Fiscal public API, then writes the returned register UUID into autonomous MiniPOS configuration. Its successful sale therefore proves the same lifecycle/registry/operator-aware execution gate used outside tests rather than relying on synthetic unregistered identifiers.
- Both Expo Web exports and BeeMiniPOS Android/iOS bundles succeed. A real Android debug APK (`assembleDebug`, 179 tasks) and an unsigned universal iOS Simulator app (`xcodebuild`, 83 targets) compile successfully with the native BLE module linked. Signed release/store builds and physical-device HIL remain release gates.
- Playwright 1.62.1 interaction E2E passes for both exported Web applications and validates MiniPOS sale/UNKNOWN/no-repeat reconciliation plus BeeFiscal readiness/report/admin journeys through public API mocks.
- Daisy SMART S STUB contract tests pass and both debug and unsigned release APK variants build with AGP 8.8.2/Kotlin 2.0.21; static/runtime guards prevent release/PROD activation.
- Contract gate resolves all local `$ref` values, rejects duplicate `operationId`, verifies router registration for all 73 canonical plus 12 runtime OpenAPI operations, and parses AsyncAPI 3.0.0. Pinned `openapi-typescript` 7.13.0 now produces versioned public/runtime surfaces; the gate regenerates them in isolation, requires byte-for-byte equality, verifies every `operationId`, and compiles `openapi-fetch` 0.17.0 typed client factories plus representative Fiscal/MiniPOS calls.
- Both Expo dependency trees have no critical/high `npm audit` findings after pinning PostCSS 8.5.23. Ten moderate Expo 53 build-tool findings remain tracked pending a controlled SDK upgrade; npm's incompatible Expo 46 downgrade is not applied.
- Three consecutive Stage 24 full regression cycles pass after the Docker test-network fix, including the tenant-bound artifact and Edge-sync ACK cutover: race/unit suites, TypeScript, 73+9 OpenAPI surface/AsyncAPI, sensitive-data and CycloneDX evidence gates, Web/Android/iOS bundles, Daisy STUB debug/release builds, C++ protocol tests, four Compose renders, real PostgreSQL RLS/rollback tests and the complete two-Compose E2E. The harness does not auto-restart services while waiting for health, so deterministic failures are not converted into flaky passes.
- On 2026-08-09, after the Stage 21 fault matrix, Stage 23 security matrix, typed-only persistence and strengthened BG evidence changes, two additional consecutive `make full-regression` runs passed on an unchanged tree. Both runs included PostgreSQL integration and the complete two-Compose sale/outage/restart/backup/restore/autonomy scenario through both Caddy entry points.
- On 2026-08-09 the full regression also passed after the registry-aware execution cutover: 19-case security matrix, race suites, PostgreSQL typed/RLS tests, platform bundles, device/protocol builds and public-API-provisioned two-Compose E2E all completed successfully.
- On 2026-08-09 `make full-regression` passed after adding the Playwright UI interaction gate; the same cycle included all race/contract/BG/driver/security tests, Expo platform bundles, PostgreSQL integration and the complete Caddy/two-Compose E2E.
- On 2026-08-09 `make full-regression` passed again after adding reproducible OpenAPI generation and typed client compilation; release evidence now inventories 755 components and the complete PostgreSQL/Caddy/two-Compose scenario remained green.
- On 2026-08-09 a unified `make full-regression` passed after the signed OTA/CAS implementation and the then-current E2E private-IP route hardening. That IP workaround was later superseded by the isolated Unix-socket design described below after Docker Desktop also demonstrated bridge blackholes.
- On 2026-08-09 a later unified `make full-regression` also passed after barcode persistence, 16-case server-driven UI action coverage, 82-operation generated OpenAPI surface and bounded Docker `dbroute` discovery. This run included Go race tests, TypeScript/unit tests, contract/BG/driver/fault/soak/security/UI/evidence gates, Web/Android/iOS bundles, Daisy STUB Android builds, C++ protocol tests, real PostgreSQL integration and the complete two-Compose sale/UNKNOWN/restart/restore/autonomy journey.
- A later full regression exposed two Docker Desktop route/bootstrap defects: a multi-homed backend intermittently could not reach a healthy PostgreSQL container, and `NetworkSettings` could omit a newly attached network. The intermediate exact-IP workaround proved insufficient when fixed bridge ranges also blackholed; the current E2E-only Unix-socket route below replaces it.
- On 2026-08-09 a unified `make full-regression` passed after implementing fiscal Z-report-backed MiniPOS shift closure. The complete cycle proves OPEN→CLOSING→CLOSED only after a final fiscal operation with both operation ID and fiscal reference; ambiguous closure enters BLOCKED_RECONCILIATION and prevents a replacement shift. The two-Compose E2E additionally verifies the successful Z-linked close, a separate UNKNOWN/no-retry sale, service restarts and restored PostgreSQL evidence through both Caddy entry points.

## Deliberately gated, not falsely implemented

- Real Daisy SMART S SDK, Daisy Compact USB/HIL, DP-150 EUR HIL, BlueCash/BluePad/acquirer, production BIM/service/legal evidence.
- These capabilities remain `EXCLUDED_FROM_MVP` or `DOCUMENTED_NOT_APPROVED` and cannot be enabled in PROD.

## Remaining before functional-complete freeze

- Explicit split tender is now an additive runtime OpenAPI operation: MiniPOS accepts an ordered 2–10 entry `payments[]`, permits only CASH/CARD, requires unique payment UUIDs and an exact EUR sum, and rejects opaque canonical `MIXED`. The in-flight request hash is persisted before any Fiscal call; deterministic downstream idempotency keys allow safe restart/replay of an interrupted sequence. Intermediate accepted tenders are exposed as canonical `EXECUTING`, while only the final exact-balance tender may produce `FISCALIZED`. Unit/race tests and the isolated two-Caddy E2E cover a EUR 1.00 CASH + EUR 1.50 CARD sale through an ACTIVE bound terminal.
- MiniPOS storno is now an append-only end-to-end path rather than a Fiscal-only capability. A completed order exposes server-owned `REVERSE`; `POST /minipos/orders/{order_id}/reversals` validates the closed Bulgarian reason allowlist, calls only the Fiscal public API with a deterministic idempotency key, preserves the original sale/operation link and records a distinct reversal operation/reference. Network ambiguity freezes the order in `UNKNOWN`; signed reversal webhook results are distinguished from sale completion and can move it only to `REVERSED`. Current enforced surface: 73 canonical + 19 runtime operations, 92 request and 92 successful-response contracts.

- BG-020 is closed through an additive runtime contract without widening the locked EUR-only canonical `ComplianceExport`. `POST /exports/periodized` uses an explicit half-open `[from,to)` interval and the pinned legal instant `2025-12-31T22:00:00Z` (`2026-01-01T00:00:00 Europe/Sofia`), producing separate immutable BGN/EUR manifests. Period and artifact GETs are tenant-scoped; domain vectors assign the boundary sale exactly once, restart preserves both artifacts, and two-Compose E2E verifies Caddy, PostgreSQL backup/restore and restored currency order.
- Typed tables are now the sole active durable representation in storage mode 2 and exact CAS delta/restart/rollback are proven. Dormant compatibility rows are retained only as a non-runtime rollback copy pending an explicitly approved destructive migration. Removal of in-process coordinator maps is a post-MVP internal refactor, not a canonical MVP gate. Historical `devices` has no runtime reader or writer and is formally excluded from MVP.
- RBAC/RLS and software provisioning enforcement are complete for the MVP boundary: central JWT/RBAC, FORCE RLS reader isolation, fail-closed device transitions, ACTIVE-only binding, active-register requirement, PROD activation evidence references and provisioning eligibility are test-covered. Actual application 24, approved-type and authorized-service documents remain external deployment evidence.
- Hardware GATT server plus USB/UART adapter wiring on the Edge devices remains. BeeMiniPOS Web and native clients activate a public-API BLE session from an explicit user gesture/permission grant, perform cloud/readiness or encrypted final-device probe before checkout, freeze REST/BLE/BLOCK routing, wait for the correlated fiscal result and never report success for UNKNOWN/BLOCKED. Chrome Web Bluetooth and native Android/iOS GATT clients plus Edge cryptography/CBOR/framing/flow/runtime/revocation/sync/OTA policy primitives are wired at the application layer; physical hardware HIL is still required.
- Runtime response enforcement now covers the complete 88-entry canonical+runtime success matrix across Fiscal, MiniPOS and Edge. A reproducible generator dereferences the locked OpenAPI schemas, assigns each operation to its owning service and emits method/path-template/status/media/JSON-Schema contracts plus the canonical Problem schema; a drift gate regenerates and compares all six Go artifacts. Each service buffers responses and fail-closes before transmission on an undocumented success route/status, absent/unexpected body, wrong media type, malformed JSON, any per-field schema violation, malformed Problem Details or a Problem body status that differs from HTTP. Binary artifacts are validated as binary media rather than decoded as JSON. Negative API/race tests cover success and error violations. This cutover exposed and fixed a Shift UUID/resource-meta mismatch and a MiniPOS checkout that returned an Order where OpenAPI requires FiscalOperation; the corrected DTO passed the complete two-Compose checkout/Z-close/outage/restart/restore journey. The canonical `createWebhookEndpoint` omission remains resolved by the hash-locked OpenAPI Overlay 1.0 and a closed typed one-time-secret DTO.
- The same generator now emits and enforces all 88 request-body contracts before business handlers execute. Required body presence, exact documented media type, 2 MiB bound and dereferenced per-field JSON Schema are fail-closed in Fiscal, MiniPOS and Edge; invalid commands cannot reach the domain/device runtime. The first run proved that 14 canonical create/update request schemas incorrectly reused response resources and therefore demanded server-owned IDs, versions, timestamps, derived state/defaults and totals. OpenAPI Overlay 1.0 now has an ordered, machine-checked 15-target allowlist: the existing webhook response correction plus nine administrative and five MiniPOS request corrections. Its SHA-256 is locked, command required fields are verified, unknown MiniPOS/Edge properties are rejected, and a complete `make full-regression` including PostgreSQL/RLS and two-Compose E2E passes with enforcement enabled.
- Generated request enforcement now also covers every documented path, query and header parameter: required values, UUID/date-time formats, const/enum values, numeric conversion and bounds are checked before handlers. Negative tests prove `limit > 200`, malformed UUIDs and missing `If-Match` cannot reach business logic. Undocumented redirects fail closed alongside undocumented success responses. This cutover exposed that canonical `addSaleLine` required optimistic concurrency while the implementation did not: Fiscal now performs an atomic repository version check, UNP allocation and line commit; MiniPOS carries the returned Fiscal sale version through every line request using `If-Match` and rejects non-sequential version responses. Race and stale-version tests prove an obsolete writer cannot mutate the sale.
- On 2026-08-09 the unified `make full-regression` passed after parameter enforcement and the resulting E2E app-instance UUID correction. The clean cycle included all three Go race suites, 88-request/88-response drift gates, BG/driver/fault/soak/security/UI/evidence checks, Web/Android/iOS exports, Daisy STUB debug/release APKs, C++, PostgreSQL/RLS and the isolated two-Compose Caddy journey with sale, Z-close, UNKNOWN/no-retry, restart, backup/restore and autonomous MiniPOS database.
- CARD execution is now registry-aware rather than controlled only by simulator availability. For an authenticated sale, Fiscal requires an ACTIVE tenant-owned PAYMENT_TERMINAL/SMART_DEVICE bound to the ACTIVE register before creating an operation; explicit CARD with `terminal_policy=NONE` is rejected and there is never cash fallback. PROD rejects the card-terminal simulator flag independently of the general STUB guard. Domain tests cover absent/bound/policy-negative paths, and two-Compose E2E provisions the optional terminal via public API and completes a MiniPOS CARD sale through both Caddy ingresses.
- Register bindings now persist and enforce their canonical `active_from`. Future-dated fiscal-device or payment-terminal bindings are not READY before the boundary; unchanged bindings retain their original activation time across register edits. Direct `fiscal_device_id`/`payment_terminal_id` fields allowed by the canonical register schema are validated against same-tenant ACTIVE devices of the correct kind, preventing clients from bypassing the binding lifecycle with unknown, foreign, inactive or wrong-kind IDs.
- Stage 25 now has versioned handover artifacts and a fail-closed verifier: release notes, a machine-readable `BG_MVP_FUNCTIONAL_NONPROD` acceptance record, operations/backup runbook, rollback plan and support guide. The record distinguishes base software PASS from PILOT/PROD NO-GO and deliberately leaves Product/Engineering/QA/Security/Compliance signatures pending; `make handover-test` rejects fabricated local signatures or accidental GO promotion.
- Complete per-command encoder/parser semantics and golden/HIL vectors beyond the now executable core receipt/report/cash/reconciliation mappings.
- BeeFiscalApp administration editors and software-verifiable exceptional/empty/loading accessibility states are implemented; physical assistive-technology interaction evidence remains part of the UI HIL gate.
- Compliance gaps that remain software-addressable are tracked row-by-row in `contracts/bg-requirements-trace.json`; hardware, vendor, payment-acquirer and legal/XSD evidence remains explicitly external and cannot be promoted by simulator tests.

## 2026-08-09 operator-session and deterministic regression update

- MiniPOS operator identity is now an executable end-to-end path: OIDC Authorization Code + PKCE in PROD, ADMIN-only immutable employee binding, app-instance-bound durable session, logout/revocation, typed PostgreSQL/RLS persistence and restart recovery. The two-Compose journey explicitly binds the E2E identity and activates the session before opening a shift.
- The OpenAPI surface is 92 operations: 73 canonical plus 19 additive runtime operations. Generated TypeScript clients, lock checksum and all-operation surface verification pass.
- Release gates now include 21 UI acceptance cases and 27 security regression cases across 12 categories. MiniPOS requires storno/reversal to be exposed only through server-owned `allowed_actions`; its touch split-tender flow validates a positive partial cash amount and derives the exact EUR card remainder. The REST checkout adapter consumes the documented `MiniPosFiscalOperation` response rather than the former browser-mock-only Order shape: `FISCALIZED` completes, known `FAILED` remains final, and only `UNKNOWN` enters reconciliation. The supply-chain case generates an Ed25519-signed evidence pack, verifies it against an independent trusted key and rejects byte tampering. The PROD browser bundle ignores a forbidden static token and remains fail-closed when no configured OIDC login can establish a server-bound operator session.
- E2E database bootstrap uses a distinct named PostgreSQL Unix-socket volume for each Compose project and verifies the socket DSN in each backend container without logging credentials. This removes Docker Desktop DNS/VPN/bridge routing from backend-to-database correctness while preserving separate PostgreSQL containers and databases. The dedicated `dbroute` remains internal, Caddy starts with `--no-deps`, only backend/Caddy join the ephemeral shared ingress, and DEV/PROD TCP topology is unchanged. Independent PostgreSQL integration tests still exercise TCP, RLS and rollback.
- Metro exports for MiniPOS specify DEV/PROD explicitly and use `--clear`; this prevents a sequential PROD export from contaminating a later DEV Web/Android/iOS bundle. The UI acceptance matrix checks these clean-cache commands.
- A clean unified `make full-regression` completed with exit code 0 after these fixes. It covered Go race tests, TypeScript and 28 MiniPOS transport tests, OpenAPI generation/lock/surface gates, BG/driver/fault/soak/security/UI/evidence gates, Web/Android/iOS bundles, Daisy STUB debug/release APKs, C++ protocol tests, all Compose configuration renders, real PostgreSQL/RLS integration and the complete isolated two-Compose sale/Z-close/UNKNOWN/restart/restore/autonomy scenario.
- The MiniPOS crypto stack is pinned to the mutually compatible noble `2.3.0` set and imports only documented `.js` export-map paths. A regression wrapper builds Web/Android/iOS with a clean Metro cache and fails if the former `@noble/hashes/crypto.js` file fallback returns. The 28 crypto/BLE/checkout tests and all three bundles pass; `js-yaml` is forced to fixed compatible `4.3.1`. Current npm advisory data still reports two root High DoS advisories against every published `image-size` version (`<=2.0.2`) reached through the Metro build tool. No fixed npm release exists at this audit, so release vulnerability status remains `PROD_NO_GO` instead of receiving a local waiver or unsafe framework downgrade.
- Stage 23 release evidence now has both fail-closed modes: the default package verifies only as `UNSIGNED_NO_GO`, while an Ed25519-signed package requires an independently supplied trusted SPKI key, exact key fingerprint, complete safe checksum inventory and an intact manifest signature. The package includes an exact-SBOM in-toto/SLSA statement; an attached scan must be fresh, completed, bound to that SBOM, internally consistent and zero Critical/High. Executable negative cases reject manifest/provenance tampering, foreign-SBOM reports and High findings. The security matrix remains 27 cases/12 categories. Signature/scan success deliberately does not override hardware/vendor or legal `PROD_NO_GO` reasons.
- Stage 24 software acceptance has two consecutive, unchanged-tree `make full-regression` runs with exit code 0 after the signed-evidence implementation. Both runs included the new trusted-key/tamper gate and the complete PostgreSQL/two-Compose journey. This proves repeatability of the current software slice; it is not a signed immutable RC because release-key custody, vulnerability report and external acceptance signatures are not present.
- The subsequent BG-020 slice adds three runtime OpenAPI operations (88 total), deterministic half-open BGN/EUR partitioning, immutable artifact digest/size binding, idempotency mismatch rejection, tenant-safe download and restart persistence. Two complete unchanged-code `make full-regression` runs passed, including real PostgreSQL integration and an expanded two-Compose Caddy journey that verifies period manifests and artifacts again after backend restart and database restore.
- The subsequent split-tender and MiniPOS reversal slices bring the enforced surface to 90 operations (73 canonical + 17 additive runtime), with 90 generated request and 90 successful-response contracts. A clean `make full-regression` completed with exit code 0 after the reversal cutover: domain/race tests, 19-case UI matrix, PostgreSQL typed reversal evidence, restart persistence and the complete two-Compose Caddy cash/card/split/reversal/UNKNOWN/backup/restore journey all passed.
- A completion audit then found that the Expo REST adapter and its Playwright mock still expected a legacy Order response from checkout while the enforced OpenAPI/backend correctly return `MiniPosFiscalOperation`. The adapter, unit tests and browser mock now use the real contract and distinguish final `FAILED` from ambiguous `UNKNOWN`. The touch UI also exposes the existing batch contract as an exact CASH+CARD split action. The resulting 31 MiniPOS transport/handshake/lifecycle tests, 22-case UI matrix, Web interaction run and complete `make full-regression` all pass.
- Receipt evidence is now durable in MiniPOS rather than existing only in the immediate checkout response. Synchronous checkout and signed online/offline Fiscal webhooks carry `fiscal_reference`; the public MiniPOS order exposes the canonical `receipt_reference`, while `008_order_fiscal_reference.sql` projects the value into the internal order and idempotent checkout result columns. A `NOT VALID` forward constraint requires it on every new `COMPLETED`/`REVERSED` row without fabricating historical data. Reversal recovery keeps original and storno references distinct. The runtime OpenAPI hash, TypeScript surface and all six Go validators were regenerated; the runtime route verifier now covers all 17 additive operations rather than 11. A complete regression proves receipt references for cash/card/split, restart and restored PostgreSQL.
- The Daisy 36-command semantic slice passed a clean `make full-regression` after the E2E route hardening. It was subsequently superseded by the 45-command slice covering every Daisy `Supported` command; the earlier result remains historical evidence rather than the current coverage count.
- The superseding 45-command Daisy slice completed a clean unified `make full-regression` with exit code 0. The cycle proved all 45 `Supported` command semantics through compiled golden/negative vectors and also reran the complete API, BG trace, driver, fault, soak, security, UI, signed-evidence, Android/iOS/Web, Daisy STUB, PostgreSQL/RLS and isolated two-Compose recovery journey.
- The subsequent Daisy optional slice completed another clean unified `make full-regression` with exit code 0. All 16 commands classified as `Optional` now have compiled typed builders/parsers plus positive and negative vectors, while retaining their optional/model-conditional activation status. The same run passed 90-request/90-response contract enforcement, BG/driver/fault/soak/security/UI/evidence gates, Android/iOS/Web builds, Daisy STUB APKs, PostgreSQL/RLS and the complete isolated two-Compose cash/card/split/reversal/Z/UNKNOWN/restart/backup/restore/autonomy journey.
- The regression entry point now also runs `go vet ./...` for Fiscal, MiniPOS and Edge using isolated writable build caches. A clean three-module vet baseline passes, and future unified regressions fail on Go static-analysis defects instead of relying only on compilation, unit and race execution.
- Production transport configuration now fails closed at process startup: Fiscal requires an HTTPS public base URL, explicit HTTPS-only CORS origin list and a 32-byte webhook signing key; MiniPOS requires an HTTPS Fiscal public API, explicit HTTPS-only CORS origins and a 32-byte webhook verification key in addition to OAuth. The PROD Compose overlays make these values mandatory, while the checked `.env.example` is an executable DEV-only profile. Wildcards, userinfo, paths/query/fragment in CORS entries, HTTP routes and weak keys have negative tests and are represented in the Stage 23 matrix.
- A clean unified `make full-regression` then passed with the new vet and production-transport gates enabled. It included 33 security cases, both checked DEV profiles and both mandatory PROD renders, all 90 request/response contracts, platform bundles, PostgreSQL/RLS integration and the complete isolated two-Compose recovery/autonomy journey.
- MiniPOS now constructs its HTTP server with bounded 5s header, 15s request-read, 30s response-write and 60s idle timeouts, matching the Fiscal resource-exhaustion boundary. An executable server-policy test and Stage 23 security case prevent regression to Go's unbounded defaults. Two consecutive unchanged-tree `make full-regression` runs passed after this fix, including 34 security cases and the full PostgreSQL/two-Compose journey required by Stage 24.
- Stage 1 no longer relies on prose-only governance evidence. A machine-checked baseline assigns owner roles to all ten mandatory module domains, proves the two products have independent databases/Compose/Caddy ownership, fixes identifier policies for requirements/tests/defects/releases and inventories P0-01..13 plus the passwordless-IdP P0. Every external P0 must retain `production_blocked=true` and existing closure evidence; the mandatory regression now runs this gate.
- The first unified regression with the governance gate in its mandatory execution path completed with exit code 0, including all downstream contract, platform, signed-evidence, PostgreSQL/RLS and two-Compose recovery checks.
- Fiscal CORS now exposes the complete browser mutation surface. The canonical API has one DELETE operation for webhook-endpoint removal, but the preflight allow-method list previously omitted DELETE, making that valid OpenAPI operation unusable from BeeFiscalApp/Web. The allowlist now includes DELETE; an executable test proves every documented public method is advertised to an approved origin and that a foreign origin receives neither a successful preflight nor CORS authority. Stage 23 now contains 35 security cases.
- All BeeMiniPOS and BeeFiscalApp public-API calls now use a bounded four-second fetch wrapper. Previously only direct MiniPOS→Fiscal calls had a deadline; a hung MiniPOS route or any hung BeeFiscal administration request could retain the UI busy state indefinitely. The shared policy composes caller cancellation, rejects invalid timeout configuration, removes listeners/timers on completion and has a real hung-request abort test. Fault coverage is 21 cases and UI acceptance is 24 cases across Android/iOS/Web.
- BeeFiscalApp production authentication is now symmetric with the MiniPOS trust boundary: Authorization Code + PKCE through a separately registered `beefiscalapp` redirect scheme, personal bearer authority only in memory, explicit logout and no API call before login. `EXPO_PUBLIC_FISCAL_AUTH_TOKEN` is DEV-only and constant-folded away from the PROD bundle. Playwright builds a production artifact with a forbidden marker, proves the marker is absent, renders the OIDC gate and observes zero pre-login API calls. Security coverage is 36 cases and UI acceptance is 25 cases.
- The unified `make full-regression` completed with exit code 0 after the BeeFiscalApp OIDC cutover: all contract/security/UI gates, Web/Android/iOS exports, Daisy STUB/C++ suites, PostgreSQL/RLS checks and the independent two-Compose/Caddy recovery journey passed together.
- Interactive OIDC sessions in both Expo apps now implement provider-driven access-token lifetime. A rotating refresh token is retained only in memory and used ahead of expiry (including retention when the provider omits an unchanged refresh token); missing refresh authority expires exactly at the access-token deadline, and refresh failure clears all API authority. Three deterministic boundary tests raise the suites to 37 security and 27 UI acceptance cases; real-IdP rotation remains deployment evidence.
- The subsequent unified full regression passed with exit code 0, proving the OIDC lifetime change together with race/vet suites, all contracts, platform exports, Daisy STUB/C++, PostgreSQL/RLS and the complete independent two-Compose/Caddy recovery journey.
- Opaque/malformed token responses no longer create unbounded UI sessions: missing lifetime metadata expires authority immediately. Production `401` responses from either public API also invalidate the personal session. MiniPOS tears down BLE READY and attempts revoke with the prior in-memory bearer before clearing both API tokens, preventing a stale operator login from leaving local fiscal execution visibly available. UI acceptance now contains 29 cases.
- The unified full regression after this auth-loss hardening completed with exit code 0 across the complete software slice, platform builds and two-Compose/Caddy PostgreSQL recovery E2E.
- MiniPOS now recovers an operator-owned open shift after browser/app or backend restart through documented `GET /minipos/shifts` employee/register/state filters. The handler requires the current identity and app-bound active operator session, rejects cross-employee recovery, and reads typed tenant storage. Auth loss clears the old local shift/cart while retaining any UNKNOWN order for later authorized reconciliation. Contract surface is 91 operations/contracts; browser reload and backend race tests pass, and the two-Compose journey asserts the open shift survives backend restart.
- The unified full regression for the 91-operation shift-recovery surface completed with exit code 0, including all 30 UI cases, generated request/response enforcement, PostgreSQL/RLS and the public-Caddy restart assertion.
- Blocked Z close is no longer terminal. `POST /minipos/shifts/{shift_id}/reconcile` reads the already persisted Fiscal operation and closes only for `FISCALIZED` plus reference; it may replay the original deterministic key only when response loss left no operation ID. Server-owned shift actions block sale in non-OPEN states. Domain proof counts one POST and one GET, while Playwright proves one close, one reconcile and no duplicate Z command. Current gates: 92 contracts, 38 security and 31 UI cases.
- The unified full regression after Z reconciliation completed with exit code 0 across race/vet, contracts, Web/Android/iOS, Daisy STUB/C++, PostgreSQL/RLS and the complete two-Compose/Caddy recovery journey.
- MiniPOS read RBAC no longer treats every tenant GET as cashier-safe. CASHIER is denied the employee directory and tenant-wide sales report; order listing resolves the active OIDC employee/app instance and filters to that employee's shifts. A two-actor API test proves the foreign order ID is absent. Production Expo avoids the employee request and hides editors for cashier roles, resetting the admin surface on authority loss. Current targeted gates: 39 security and 32 UI cases.
- The unified full regression after the cashier least-privilege increment completed with exit code 0, including race/vet, all 92 request/response contracts, 39 security and 32 UI cases, Web/Android/iOS exports, Daisy STUB/C++, PostgreSQL/RLS and the complete independent two-Compose/Caddy recovery journey.
- A roadmap evidence audit found that Fiscal GET authorization and BeeFiscalApp navigation were still broader than the Stage 17 role boundary. Tenant-wide operator, audit, webhook, report/export and provisioning reads are now explicitly role-separated. BeeFiscalApp derives visible tabs/actions from fail-closed access-token roles: ADMIN alone sees editors, AUDITOR is report-read-only, and report execution/reconciliation requires SUPERVISOR or ADMIN. Opaque/malformed role claims expose no privileged controls, with a direct TypeScript test. The resulting unified regression passed with 40 security and 33 UI cases, all platform exports, PostgreSQL/RLS and the complete two-Compose/Caddy journey.
- The same audit found that the release gate's “both apps / Android+iOS+Web” claim was stronger than its command: native Expo exports covered MiniPOS only. `native-bundle` now creates and asserts Android and iOS BeeFiscalApp bundles independently. The first new Android gate caught an incompatible ESM package-mode change (`module is not defined`); the change was removed and both native BeeFiscalApp exports pass.
- Stage 17 device administration is now functionally complete rather than inferred from generic device CRUD. BeeFiscalApp exposes role-gated capability/diagnostics reads, provisioning-session creation and REST-issued BLE-session preparation bound to a supplied X25519 client public key. Cloud/Edge transport and final-device reachability are retained and rendered as separate metric groups. The device editor and binding flow explicitly distinguish `FISCAL_DEVICE` from `PAYMENT_TERMINAL` / `OPTIONAL_PAYMENT_TERMINAL`. A real browser executes all four control-plane calls and switches the optional-terminal capability; UI acceptance now contains 34 cases.
- The MiniPOS/Fiscal product boundary is now executable rather than a Compose convention. MiniPOS validates an exact HTTP(S) `/public/v1` base in every environment and rejects credentials, query, fragment, trailing/internal paths and file/database schemes; request-construction errors are returned rather than dereferencing a nil request. `boundary-test` scans versioned Compose service blocks, database mounts, private networks, imports and downstream path literals, and is mandatory in regression and Stage 15 acceptance. Security coverage is 41 cases.
- Canonical compliance exports no longer strand their generated artifact behind a manifest-only API. The existing public tenant-scoped artifact route now downloads both canonical and periodized JSON/CSV/XLSX, binds the requested artifact ID to its owning export and supplies matching MIME plus filename extension. Cross-tenant and substituted artifact IDs fail as not found. Canonical intervals are now strictly half-open `[from,to)`, rejecting zero width and preventing a boundary sale from appearing in adjacent periods. Direct API tests cover all three formats and the two-Compose Caddy journey downloads and validates a real XLSX ZIP. Security coverage is 42 cases.
- The MiniPOS Fiscal webhook request is no longer an open-ended object. Runtime OpenAPI now closes both envelope and `data`, documenting `occurred_at`, operation type, sale/external IDs, fiscal reference and error evidence while rejecting unknown fields before signature-verified business processing. A negative API test proves an otherwise correctly signed event with an undocumented evidence member fails as `REQUEST_CONTRACT_VIOLATION`.
- Completion is now checked against all 25 canonical roadmap stages by `contracts/roadmap-stage-acceptance.json` and `make roadmap-acceptance-test`: 19 stages are software PASS, five hardware/vendor/legal stages are formally excluded with explicit closure evidence, and Stage 25 remains pending external signatures. During this audit, response enforcement was found to stop at the first schema when canonical and additive OpenAPI described the same method/path/status. MiniPOS middleware now validates every matching schema, so `GET /minipos/orders/{order_id}` cannot omit runtime-owned `allowed_actions` while still passing the canonical order schema; a negative API test locks this behavior.
- The Stage 25 acceptance record is now executable rather than numerically stale: `make handover-test` derives 73 canonical + 19 runtime operations, all 92 generated request and 92 successful-response contracts, and all BG status totals from authoritative artifacts. A negative drift test mutates the acceptance count and proves the handover gate fails closed instead of blessing an obsolete release record.
- REST-issued BLE authority is now operator-bound at the Fiscal source of truth. Initial issuance rejects unknown, cross-tenant, future and expired operators, while refresh revalidates the stored operator and fails after deactivation; active register and end-device gates remain mandatory. Domain and public API negative tests prevent an authenticated caller from obtaining or extending `fiscal.execute` authority with an invalid operator code.
- MiniPOS no longer ships the non-contractual `FD000001`/derived-device aliases. Runtime OpenAPI, generated validators, autonomous configuration and shift DTOs now require canonical UUIDs; the touch app validates register/device identity before configuration, shift, readiness or BLE calls and remains fail-closed when deployment identity is absent. Targeted TypeScript, Go, contract and browser gates pass; UI acceptance is 39 cases.
- The unified full regression after the MiniPOS UUID cut completed with exit code 0, including race/vet, all contract/trace/security/UI gates, Android/iOS/Web exports for both apps, Daisy STUB/C++, PostgreSQL/RLS and the independent two-Compose/Caddy recovery/autonomy journey.
- MiniPOS cloud readiness no longer depends on a hidden `EXPO_PUBLIC_FISCAL_DEVICE_ID`. It resolves the ACTIVE register through `GET /registers/{register_id}`, validates the returned `fiscal_device_id`, then probes that device; mismatch, inactive/unbound registry state and malformed IDs block before sale. A device env fallback remains DEV-only. Unit and real-browser evidence pass; UI acceptance is 40 cases.
- The full regression for public register-to-device resolution completed with exit code 0 across contracts, race/vet, security/fault/UI matrices, both-app platform exports, device/STUB suites, PostgreSQL/RLS and the isolated two-Compose/Caddy journey.
- Missing workstation configuration no longer broadens shift recovery. MiniPOS calls the filtered recovery API only for a UUID-valid configured register; otherwise it clears shift/cart state and requires configuration. Browser evidence proves no shift-list request is emitted when configuration is missing. UI acceptance is 41 cases.
- The scoped shift-recovery change passed the complete unified regression with all contract, platform, storage and isolated two-Compose/Caddy gates.
- Concurrent MiniPOS shift close is now serialized by a state recheck under the domain mutex. Previously two callers could both pass the pre-lock OPEN read and emit duplicate Z commands with the same remote idempotency key. A blocked-first-request race test proves exactly one Fiscal POST and one successful close; fault coverage is 22 cases.
- The concurrent Z-close fix passed the complete unified regression, including Go race/vet and the public two-Compose Z-linked shift-close journey.
- Reversal now freezes the order durably as `UNKNOWN` before any Fiscal network call. This closes a different-key concurrency hole and makes crash/timeout state conservative; only final `FISCALIZED` becomes `REVERSED`, while final `FAILED/CANCELLED` restores `COMPLETED` for an explicit retry. A held-first-call race test proves exactly one storno reaches Fiscal. Fault coverage is 23 cases.
- The crash-safe reversal freeze passed the complete unified regression, including append-only reversal reporting, PostgreSQL restart/restore and two-Compose/Caddy storno evidence.
- Reversal completion now handles webhook-before-response ordering. If the signed webhook has already committed the same operation as `REVERSED`, the later synchronous response is accepted idempotently instead of surfacing a false ambiguous error. The existing public reversal test now injects this ordering under `-race`; fault coverage is 24 cases.
- The reversal webhook-ordering fix passed the complete unified regression, including signed webhook gates, reporting, restart/restore and append-only storno E2E.
- Fiscal payment execution now has an authoritative durable reservation: repository CAS verifies the sale version and `OPEN` state, persists `PAYMENT_PENDING` plus the `EXECUTING` operation atomically, and only then invokes the driver. A blocked-driver race test proves a second full payment cannot pass balance/state checks or touch the device. Fault coverage is 25 cases.
- The durable Fiscal payment-reservation cut passed the complete unified regression with exit code 0, including race/vet, 92/92 generated contracts, fault/security/UI matrices, both-app Android/iOS/Web exports, Daisy STUB/C++, PostgreSQL/RLS and independent two-Compose/Caddy outage/recovery evidence.
- Final Fiscal payment state is now committed atomically with its immutable receipt artifact. If persistence fails after the device has answered, no completed sale, payment or orphan receipt leaks; the durable `PAYMENT_PENDING`/`EXECUTING` reservation remains. Repository restart converts that interrupted pair to `UNKNOWN` with `RECONCILE` and never replays the driver command. Fault coverage is 26 cases.
- The atomic payment-finalization and interrupted-payment recovery cut passed the full unified regression with exit code 0, including PostgreSQL/RLS and independent two-Compose/Caddy restart evidence.
- Startup recovery now applies to every durably interrupted `EXECUTING` fiscal command, including reversal and reports, not only sale payment. Each becomes `UNKNOWN` with the sole `RECONCILE` action and no device replay; payment reservations additionally move to sale `UNKNOWN`. Fault coverage is 27 cases.
- Generalized interrupted-command recovery passed the complete unified regression with exit code 0 across all software, platform, persistence and isolated two-product recovery gates.
- Direct Fiscal storno now reserves the completed sale with an atomic version CAS as canonical `FISCALIZATION_PENDING` plus durable `EXECUTING` operation before touching the driver. Competing storno requests cannot both execute; known failure restores `COMPLETED`, ambiguity commits sale and operation `UNKNOWN`, and success uses the locked Fiscal Sale state `CANCELLED` while the operation/webhook retain explicit reversal semantics.
- X/Z, cash movement and device-management fiscal commands now also persist `EXECUTING` before driver dispatch. A failed durable write blocks the side effect. Restart evidence proves an interrupted storno moves both sale and operation to `UNKNOWN/RECONCILE`. Fault coverage is 30 cases.
- Durable storno and generic-command reservation passed the complete unified regression with exit code 0 across race/vet, contracts, all regression matrices, platform builds, device suites, PostgreSQL/RLS and two-Compose/Caddy recovery.
- Repository rollback now restores a genuinely empty confirmed snapshot instead of leaving the first failed mutation visible in memory. Canonical and periodized compliance exports publish resource manifest, operation, all immutable artifacts and audit evidence in one persistence transaction; injected failure exposes none of them. Fault coverage is 32 cases.
- Empty-snapshot rollback and atomic compliance-export publication passed the complete unified regression with exit code 0, including PostgreSQL typed delta/RLS and the two-Compose/Caddy canonical plus periodized BGN/EUR export/restart journey.
- Report commands now use a single lifecycle: durable `EXECUTING`, exactly one driver dispatch, then either a persisted terminal failure/`UNKNOWN` without report publication or one atomic `FISCALIZED` operation + report manifest + immutable artifact + audit commit. Injected post-device storage failure leaves only the reservation and restart recovers it as `UNKNOWN/RECONCILE`; an ambiguous device result can no longer fabricate a `COMPLETED` report. Fault coverage is 34 cases.
- Atomic report lifecycle passed the complete unified regression with exit code 0, including response contracts, Z-report API behavior, PostgreSQL/RLS and independent two-Compose/Caddy restart evidence.
- Fiscal sale payment and reversal terminal commits now include their version-bound webhook outbox item in the same repository transaction as sale, operation and optional receipt artifact. The HTTP layer no longer performs a fallible second queue write after the irreversible result. Injected final-commit failure exposes neither terminal state nor event, while explicit legacy queue retries remain idempotent. Fault coverage is 35 cases.
- Atomic terminal fiscal state/outbox passed the complete unified regression with exit code 0, including webhook dispatcher/idempotency, PostgreSQL/RLS and independent two-Compose/Caddy payment, reversal and restart evidence.
- Webhook completeness now covers every generic X/Z, cash and device-management command with an atomic version-bound `fiscal.operation.updated`. Successful reports atomically publish both that operation event and the declared `register.report.completed` event with their operation/resource/artifact commit; failed publication leaks neither. Fault coverage is 37 cases.
- Generic-command and dual report webhook completeness passed the complete unified regression with exit code 0 across dispatcher/idempotency, contracts, PostgreSQL/RLS, all platform/device suites and independent two-Compose/Caddy Z-report/restart evidence.
- Fiscal reconciliation now atomically commits `UNKNOWN → RECONCILING`, audit evidence and the declared version-bound `fiscal.operation.reconciliation_required` outbox event. Its payload explicitly records `lookup_only=true`; the endpoint has no result body and never replays a command. Injected persistence failure rolls both state and event back to the original `UNKNOWN`. Fault coverage is 38 cases.
- Atomic lookup-only reconciliation start passed the complete unified regression with exit code 0 across API/UI allowed-actions, dispatcher/idempotency, PostgreSQL/RLS, all platform/device suites and independent two-Compose/Caddy recovery.
- Fiscal operations now persist their authoritative `register_id` across cloud and Edge ingestion. Shift close checks unresolved `EXECUTING`, `UNKNOWN` and `RECONCILING` work only for the same tenant/register; another tenant or workstation cannot block it. Legacy sale operations derive the register from their durable sale and remain fail-closed within the correct tenant. Fault coverage is 40 cases; security coverage is 43 cases.
- Tenant/register-scoped shift isolation passed the complete unified regression with exit code 0 across race/security gates, PostgreSQL typed/RLS restart, Edge/device suites, all app platforms and independent two-Compose/Caddy shift recovery.
- OpenShift uniqueness is now atomic and aligned across domain and PostgreSQL: exactly one `OPEN`, `CLOSING` or `RECONCILIATION_REQUIRED` shift may exist per `(tenant_id, register_id)`, while the same external register identifier in another autonomous tenant is independent. A migration backfills indexed operation register identity and replaces the old OPEN-only partial index. Fault coverage is 42 cases.
- Atomic OpenShift uniqueness and migration 006 passed the complete unified regression with exit code 0, including clean PostgreSQL initialization, typed/RLS integration, concurrent domain race proof and independent two-Compose/Caddy shift recovery.
- Fiscal sale identity is now enforced consistently before persistence and by the typed PostgreSQL constraints: `(tenant_id, external_id)` and non-empty `(tenant_id, UNP)` have one owner. Concurrent requests with independent idempotency keys produce exactly one sale, while different tenants remain autonomous. Recovered snapshots with a lagging UNP counter scan issued tenant numbers and advance without reuse. Fault coverage is 44 cases.
- The sale-identity/UNP cut passed the complete unified regression with exit code 0: 92/92 OpenAPI contracts, Go race/vet, 44 fault, 43 security and 41 UI cases, clean PostgreSQL typed/RLS integration, all Android/iOS/Web builds, IoT/device suites and independent two-Compose/Caddy recovery E2E.
- Fiscal and MiniPOS mutation idempotency now reserve a durable `PENDING` claim before routing to business or device logic. Versioned-store refresh makes the claim visible across backend replicas; exactly one owner executes, an in-flight loser receives retryable conflict, and a completed response is replayed byte-for-byte. Ambiguous 5xx responses remain pending but are durably replayed with their original status/body, preventing a retry from repeating a possible fiscal side effect. Migrations Fiscal 007 and MiniPOS 010 project the claim state in typed PostgreSQL. Fault coverage is 48 cases.
- The cross-replica idempotency cut passed the complete unified regression with exit code 0 after an integration bugfix preserving the established outage contract: 92/92 OpenAPI contracts, Go race/vet, 48 fault, 43 security and 41 UI cases, clean PostgreSQL migrations/RLS, all platform/device builds and independent two-Compose/Caddy ambiguous-outage/restart recovery.
- Startup recovery now makes a claim orphaned before any HTTP result explicit without ever reclaiming it: an empty `PENDING/102` becomes a durable, replayable `503 IDEMPOTENCY_OUTCOME_UNKNOWN` problem in both Fiscal and MiniPOS. Clients and operators receive deterministic evidence after restart, while recovery remains lookup/reconciliation-only and cannot blindly repeat a possible device or downstream command. Fault coverage is 50 cases.
- Orphan-claim recovery passed the complete unified regression with exit code 0: 92/92 contracts, Go race/vet, 50 fault, 43 security and 41 UI cases, PostgreSQL typed/RLS restart, all Android/iOS/Web and device suites, plus independent two-Compose/Caddy outage, restart and backup/restore.
- Public mutation idempotency now fingerprints the complete command context, not only its JSON body: canonical query parameters, `If-Match`, `X-App-Instance-Id` and the body use deterministic length-prefixed framing in both Fiscal and MiniPOS. Reusing one key with a different condition, route query or app authority is rejected as a conflict and cannot replay an unrelated result. Fault coverage is 52 cases.
- The complete-request-fingerprint cut passed the unified regression with exit code 0: 92/92 contracts, Go race/vet, 52 fault, 43 security and 41 UI cases, clean PostgreSQL typed/RLS migrations, Android/iOS/Web builds for both applications, device suites, and independent two-Compose/Caddy outage, restart and backup/restore E2E.
- Durable idempotent replays now preserve the original representation metadata needed by a client: `Content-Type`, `ETag`, `Location`, `Retry-After` and `Cache-Control`. A strict allow-list prevents `Set-Cookie`, authentication material, CORS state and arbitrary handler headers from becoming durable or crossing sessions. Legacy replay records remain readable through the existing `content_type` fallback. Fault coverage is 54 cases.
- The representation-header replay cut passed the complete unified regression with exit code 0: 92/92 contracts, Go race/vet, 54 fault, 43 security and 41 UI cases, PostgreSQL typed/RLS migration and restart tests, all Android/iOS/Web and device builds, and isolated two-Compose/Caddy outage, restart and backup/restore E2E.
- Fiscal admin business identities are now atomic at the repository boundary and mirrored by PostgreSQL partial unique indexes: one organization per tenant; case-insensitive location/register/operator code per tenant and kind; case-insensitive device serial per tenant. Concurrent creates have one winner in memory, cross-replica writes remain fenced by snapshot CAS, and different tenants remain autonomous. Fault coverage is 55 cases.
- Fiscal resource `If-Match` is now a real commit-time CAS instead of a check-before-write: the repository accepts only version 1 for creation and exactly `stored+1` for update under the same lock. Resource reads/writes clone mutable data maps, preventing binding or webhook code from changing authoritative state before a successful commit. Same-version concurrent updates therefore have one winner with no lost update. Fault coverage is 56 cases.
- The resource identity/CAS cut passed the complete unified regression with exit code 0: 92/92 contracts, Go race/vet, 56 fault, 43 security and 41 UI cases, clean PostgreSQL typed/RLS migration tests, every Android/iOS/Web and device build, and isolated two-Compose/Caddy outage, restart and backup/restore E2E.
- Fiscal and MiniPOS idempotency fingerprints now include the verified OIDC issuer and subject. A refreshed token for the same principal continues to replay safely, while another authorized user in the same tenant cannot retrieve or attribute the original mutation result by reusing its key. No bearer token or raw credential is persisted. Coverage is 58 fault and 45 security cases.
- Both RS256 OIDC verifiers now propagate the already validated, trailing-slash-normalized issuer into the authorization claims context. This closes a production-only identity-resolution defect where MiniPOS could validate `iss` cryptographically but then lose it before immutable employee binding lookup; verifier tests assert the canonical issuer value.
- The principal-bound replay/OIDC issuer cut passed the complete unified regression with exit code 0: 92/92 contracts, Go race/vet, 58 fault, 45 security and 41 UI cases, clean PostgreSQL typed/RLS tests, all Android/iOS/Web and device builds, and independent two-Compose/Caddy outage, restart and backup/restore E2E.
- MiniPOS identity binding and operator-session creation now revalidate the employee as tenant-owned and `ACTIVE` while holding the mutation lock. Issuers are canonicalized consistently with verified OIDC claims. Product/employee creation derives `active` from `status` rather than forcing true. Migration 011 repairs mutable legacy projections/payloads and enforces status/active equivalence plus case-insensitive SKU/operator keys. Coverage is 59 fault and 47 security cases.
- The MiniPOS identity-authority/catalog-consistency cut passed the complete unified regression with exit code 0: 92/92 contracts, Go race/vet, 59 fault, 47 security and 41 UI cases, clean PostgreSQL typed/RLS migration tests, all Android/iOS/Web and device builds, and independent two-Compose/Caddy outage, restart and backup/restore E2E.
- Fiscal and MiniPOS line totals now use checked fixed-point integer arithmetic end to end. Fiscal accepts the canonical one-to-three decimal quantity forms, both products round half cents deterministically, negative sale prices are rejected, and multiplication, aggregate and paid-balance overflow fail before persistence or device dispatch. Coverage is 61 fault cases.
- The checked-arithmetic cut passed the complete unified regression with exit code 0: 92/92 contracts, Go race/vet, 61 fault, 47 security and 41 UI cases, clean PostgreSQL typed/RLS migrations, all Android/iOS/Web and device builds, and independent two-Compose/Caddy outage, restart and backup/restore E2E.
- Line discount is now a first-class absolute EUR value across MiniPOS order storage, exact totals, the effective OpenAPI `SaleLine`, Fiscal validation, immutable command serialization and BLE/offline synchronization. Negative, foreign-currency and over-gross discounts fail before persistence or device execution; both autonomous databases enforce matching constraints.
- MiniPOS exposes manager-only touch controls for both percentage and exact-amount discounts; the backend independently requires `SUPERVISOR` or `ADMIN` for every non-zero discount. The complete slice passed unified regression with 92/92 contracts, 64 fault, 48 security and 42 UI cases, all target bundles/device suites, clean PostgreSQL/RLS and two-Compose/Caddy outage/restart/backup-restore E2E.
- Compliance artifacts are no longer identifier-only rows. JSON, CSV and XLSX export immutable line snapshots, discounts, tax groups, exact net totals, payments, fiscal operation and receipt-artifact linkage. Periodized export validates every stored monetary currency against the legal BGN/EUR period and fails closed rather than relabeling amounts.
- Fiscal payment reservation now atomically captures the exact end-device identity (`device_id`, serial, FMIN/FM, vendor/model/firmware). Receipt artifacts, public receipt and detailed exports use that immutable snapshot after later register rebinds. PROD fiscal-device activation requires both FMIN and FM; migration 010 normalizes and indexes the snapshot in typed PostgreSQL while retaining the exact payload.
- Compliance export requests now expose OpenAPI-defined `location_id` and `device_id` filters. Online sales freeze location at creation and end-device identity at payment reservation; BLE materialization carries the signed command location. Migration 011 normalizes the location snapshot, and JSON/CSV/XLSX evidence includes it without consulting mutable register configuration. Fault coverage is 69 cases.
- The immutable historical-filter cut passed the full unified regression with exit code 0: 92/92 OpenAPI contracts, Go race/vet, 69 fault, 49 security and 42 UI cases, clean PostgreSQL/RLS migrations, Android/iOS/Web bundles, Smart/IoT suites and independent two-Compose/Caddy outage/restart/backup-restore E2E.
- BLE authority now distinguishes `edge_id` from the actual bound `device_id`. Issuance freezes tenant/location/register/Edge/final-device/operator/app/principal in the signed ticket; MiniPOS places the location in the hashed offline sale payload and addresses the real fiscal device. Refresh fails after register location/device rebinding. OpenAPI makes location/register mandatory and migration 012 provides normalized indexed evidence. Coverage is 70 fault and 50 security cases.
- The corrected BLE identity cut passed the full unified regression with exit code 0 after the downgrade guard: 92/92 contracts, Go race/vet, 70 fault, 51 security and 42 UI cases, clean PostgreSQL/RLS, Android/iOS/Web, Daisy SMART STUB, IoT and independent two-Compose/Caddy outage/restart/backup-restore E2E. Edge rejects any signed legacy ticket missing the immutable location/final-device context.
- MiniPOS and Edge now use the same complete tenant/location/register/Edge/final-device/session/protocol handshake context. An exact cross-language golden vector prevents silent key-derivation drift, and MiniPOS rejects any outer REST session identity or client public key that differs from the signed ticket before BLE connection. Security coverage is 53 cases.
- The cross-language BLE-context cut passed the complete unified regression: 92/92 OpenAPI request and response contracts, Go race/vet, 70 fault, 53 security and 42 UI cases, PostgreSQL/RLS, Android/iOS/Web, Daisy SMART STUB, IoT and the independent two-Compose/Caddy outage/restart/backup-restore journey all completed successfully.
- BLE discovery now consumes the REST-issued `advertising_identity` as authority instead of selecting the first same-service peripheral. Web disconnects a user-selected mismatch before GATT discovery; Android/iOS continue scanning past foreign same-service devices until the exact local identity appears. The signed ticket/HELLO proof remains the cryptographic authorization boundary. MiniPOS coverage is 48 tests and security coverage is 54 cases.
- The advertising-identity discovery cut passed the complete unified regression: 92/92 OpenAPI request/response contracts, Go race/vet, 70 fault, 54 security and 42 UI cases, PostgreSQL/RLS, Android/iOS/Web, Smart/IoT and the independent two-Compose/Caddy outage/restart/backup-restore journey completed successfully.
- Discovery authority is now chained before scanning: MiniPOS first validates the REST Edge identity against the ticket payload, then requires `advertising_identity == edge_id`, and only then asks Web/native BLE to select that exact peripheral. This prevents an unsigned detached discovery value from steering the client before ticket binding. MiniPOS coverage is 49 tests and security coverage is 55 cases.
- The pre-scan authority-chain cut passed the complete unified regression: all 92 request/response contracts, Go race/vet, 70 fault, 55 security and 42 UI cases, PostgreSQL/RLS, Android/iOS/Web, Smart/IoT and independent two-Compose/Caddy recovery completed successfully.
- `BleSession` now exposes mandatory `tenant_id` in the effective OpenAPI contract and every issue/refresh response. MiniPOS compares it with both the ticket tenant and its active deployment tenant before storing local authority; tenant keys remain non-empty OIDC tenant strings rather than being incorrectly narrowed to UUIDs. Generated TypeScript and all Go response validators were regenerated.
- The BLE tenant-contract cut passed the complete unified regression: 92/92 request/response contracts, Go race/vet, 70 fault, 55 security and 42 UI cases, PostgreSQL/RLS, Android/iOS/Web, Smart/IoT and independent two-Compose/Caddy recovery completed successfully.
- Before issuing BLE authority, MiniPOS now resolves the ACTIVE public register binding and freezes the expected final fiscal device. It rejects any returned session whose tenant, register or device differs instead of overwriting response identities with local configuration; a concurrent rebind therefore requires a clean retry. MiniPOS coverage is 50 tests and security coverage is 56 cases.
- The MiniPOS BLE deployment-binding cut passed the complete unified regression: 92/92 contracts, Go race/vet, 70 fault, 56 security and 42 UI cases, PostgreSQL/RLS, Android/iOS/Web, Smart/IoT and independent two-Compose/Caddy recovery completed successfully.
