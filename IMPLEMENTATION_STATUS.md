# MVP implementation status

## Implemented

- Fiscal public HTTP vertical: versioning, idempotency, sale, first-line УНП, EUR/tax validation, cash/card, reversal, readiness, shifts, cash movement, X/Z/KLEN/FM report operations, connectivity, BLE session resource and edge sync ACK.
- Fiscal polling surface includes tenant-filtered operation listing, sale cancellation, normalized receipt retrieval and explicit UNKNOWN→RECONCILING transition.
- Fiscal administrative surface now persists tenant-isolated organizations, locations, registers, operators and devices with UUIDs, optimistic versions, uniqueness/reference validation, device bindings, capability manifests, provisioning sessions and redacted diagnostics.
- Device activation now fails closed from creation onward: every new device must start in `DRAFT`, cannot skip `PENDING_SERVICE_ACTIVATION`, only ACTIVE devices may bind to an ACTIVE register, ACTIVE devices cannot request a bootstrap provisioning session, and an ACTIVE PROD device requires both approved-type and service-contract evidence references. These controls validate evidence presence without claiming the external evidence is genuine.
- Created provisioning sessions are now durable tenant-bound resources, survive repository and real PostgreSQL typed-only restart and participate in the normal audit/persistence path. The storage-mode-2 integration test proves their exact payload/tenant/state projection and reconstruction. Cross-tenant reads fail closed; non-provisionable device state returns explicit HTTP 409 `DEVICE_NOT_PROVISIONABLE`, while an absent/foreign device remains 404.
- Shifts are tenant-scoped and support list/get/open/close. Reports have persistent immutable JSON artifacts and list/get/download endpoints; resource and fiscal mutations append a persistent hash-chained audit ledger exposed read-only.
- Compliance exports persist real tenant-filtered JSON, CSV or minimal standards-compliant XLSX artifacts with SHA-256 manifests; invalid periods/types/formats and cross-tenant reads are rejected.
- Public API enforces the OpenAPI 16–255 character idempotency-key constraint; receipts include operation, total, issue time and immutable artifact identifiers.
- Effective-dated BG policy endpoints (`/country-policy`, `/tax-groups?effective_at=`) now expose the EUR policy and reject dates without an active policy. Sale lines are checked against the reviewed policy catalog; only the evidenced MVP B/20.00 seed is executable, while unreviewed A/C–H mappings fail closed pending a signed policy update.
- Fiscal list surfaces now implement the contract cursor/limit bounds with opaque cursors and `PageMeta`; operations support state/register filters, reports support register filtering, and audit events support actor/УНП/period filtering with strict RFC3339 period validation.
- Deterministic non-production simulator with hard PROD configuration guard.
- MiniPOS autonomous domain/API: employees/products with optimistic updates, shifts, orders, lines, checkout and reports; Fiscal is accessed only through its public HTTP API.
- MiniPOS now persists and exposes its autonomous single-location/single-workstation configuration with optimistic versioning. BeeMiniPOS edits location name/address, workstation name and the bound Fiscal register through the MiniPOS public API; changing the binding invalidates the active local BLE session in the UI.
- Fiscal and autonomous MiniPOS now persist every repository collection entry as an independent PostgreSQL JSONB row in one serializable transaction, each with its own authoritative generation marker and automatic one-time migration from the former aggregate snapshot. Empty authoritative state cannot resurrect legacy data; sales/orders, outbox/inbox, idempotency and sequence state survive restart in their isolated databases.
- Edge authority lease/fencing allocation, durable hash-chained SQLite WAL journal, three-month ACK-gated GC, encrypted directional BLE sessions and durable-before-device runtime.
- Edge sync now builds the full OpenAPI batch from unacknowledged SQLite events (maximum 100): normalized device events, contiguous journal sequence, previous committed hash, per-event chain, batch SHA-256 and HMAC signature. Fiscal accepts JSON and CBOR, verifies every layer before durable commit, and returns the complete signed acknowledgement with committed event hash, operation results and rejected array. Before the first network side effect, the uploader persists the exact pending first/last sequence and batch SHA-256 in SQLite. Ambiguous response loss, newly appended events and process restart therefore resend the identical idempotency key/body instead of expanding the batch and duplicating a backend commit. A valid bound ACK atomically advances cursor, acknowledges journal rows and clears the pending marker. A shared deterministic golden vector proves Edge builder/Fiscal verifier compatibility.
- An executable accelerated soak gate models all 864 five-minute slots across 72 hours of clean outages/ambiguous-after-commit losses and all 1008 ten-minute slots across seven journal days. It reopens SQLite daily, verifies zero lost events and duplicate backend commits, exact ACK replay, final cursor/hash, chain integrity, three-month ACK-gated retention, purge anchor continuity and produces a machine-readable Go JSON report. This is software time-model evidence only and does not promote physical SD/HIL endurance.
- Accepted offline `FISCAL_SALE` commands are persisted by Fiscal and may be completed by a later sync batch after either side restarts. A terminal device result atomically materializes the tenant sale, operation, normalized receipt artifact and webhook/audit outbox event; malformed currency, totals, operator, tax, payment or tenant/device bindings fail closed. Same-batch acceptance/completion and split-batch correlation are idempotent.
- Signed Fiscal webhook outbox with retry/backoff and MiniPOS HMAC receiver; polling remains available as the authoritative fallback.
- Fiscal→MiniPOS/Edge webhook delivery now follows the canonical `t,kid,v1` signature over `timestamp.raw_body`, and both receivers enforce constant-time HMAC plus the 300-second replay window before JSON parsing. Delivery failures are isolated per endpoint, use the documented 10s/30s/2m/10m/1h/6h/24h schedule, and HTTP 410 durably disables a dynamic endpoint instead of retrying it.
- Dynamic tenant-scoped webhook endpoint create/get/update/disable/secret-rotation lifecycle is persistent and idempotent. Secrets are disclosed only once, GET is redacted, a 24-hour previous-secret overlap is recorded, URL/event validation fails closed, and the dispatcher selects active endpoints by tenant/event and signs with each endpoint secret. Per-endpoint delivery checkpoints prevent replaying already successful destinations when another destination retries; the egress client disables proxies/redirects and rejects private/loopback/link-local DNS answers at connect time.
- MiniPOS checkout validates malformed Fiscal responses without panic and persists tenant-scoped idempotency request hashes; a reused key with a different payload is rejected.
- Every public MiniPOS mutation now has a durable tenant/path/method scoped idempotency record with request hash, exact response replay, restart recovery and mismatch rejection.
- MiniPOS products/employees/shifts/orders now emit UUID/resource metadata, status/unit/roles, exact EUR totals and timestamps required by OpenAPI; line writes enforce optimistic `If-Match`, UUID, quantity/tax/money and same-tenant product ownership.
- Signed Fiscal events are first durably stored with their raw body in the autonomous MiniPOS webhook inbox, then applied by monotonic fiscal version; event replay and event-ID payload substitution are handled safely across restart.
- Offline Edge sale webhooks carry `external_id`; MiniPOS links them to the locally created order even before a cloud sale ID exists, persists the Fiscal sale/operation IDs and applies monotonic terminal state. This closes eventual reconciliation of a successful BLE checkout into the autonomous MiniPOS database.
- BLE session issue/refresh/revoke is durable, tenant-scoped, signed and exposes the canonical OpenAPI field names and GATT UUIDs. Issue now fails closed unless the requested register belongs to the tenant, is ACTIVE and is bound to an ACTIVE tenant-owned fiscal/smart device; refresh re-evaluates that binding so a subsequently BLOCKED device cannot extend local authority.
- Registry authority is now enforced at every central execution boundary: authenticated sale creation, shift opening, payment/fiscal sale, reversal, cash movement and report commands require the tenant-owned ACTIVE register plus its ACTIVE fiscal/smart-device binding. The check runs before driver invocation. Reversal and generic report/cash operations now execute through the driver and persist `FISCALIZED`, `FAILED` or `UNKNOWN/RECONCILE` instead of manufacturing a direct success reference in the service layer.
- Authenticated sale creation and shift opening additionally require an operator code registered in the same tenant whose `active_from`/`active_to` interval contains the current instant. Missing, cross-tenant, not-yet-active and expired operators fail closed. Fiscal operator authority remains separate from the autonomous MiniPOS employee record and is provisioned through the Fiscal public API.
- BLE Edge primitives now include canonical RFC 8949 deterministic CBOR, strict decoding, payload hashing, X25519/HKDF-SHA-256 key agreement, AES-256-GCM directional counters, exact binary GATT framing and MTU 185/247/517 chunk tests.
- BLE flow control now enforces one in-flight message, ACK state (`highest_contiguous`/missing bitmap), duplicate consistency, BUSY, cancel and resume; an integration test carries a canonical encrypted/chunked CBOR sale through reassembly into the durable Edge runtime and proves device exactly-once replay.
- Edge now implements the complete ticket-validated handshake state machine: HELLO binding/scope/expiry checks, X25519/HKDF-SHA-256, AES-256-GCM encrypted AUTH_PROOF, constant-time proof validation, READY and replay rejection, producing the directional encrypted transport session only after success.
- MiniPOS has an executable REST/BLE/BLOCK decision model with separate transport/device metrics and tests for cloud loss, final-device loss and session expiry. Chrome Web Bluetooth performs secure-context/user-gesture discovery and the matching HELLO→CHALLENGE→AUTH_PROOF→READY WebCrypto handshake; fiscal execution remains unavailable until READY. After READY it canonical-CBOR encodes commands, splits them for the negotiated ATT MTU and emits the exact encrypted BLE v1 binary frames with directional key derivation and monotonic counters. Incoming directional frames enforce tag/counter/header checks and are gap-aware reassembled with one-message BUSY and duplicate-consistency rules before reaching the UI; encrypted ACK/NACK progress is returned through the Flow characteristic.
- MiniPOS checkout transport now freezes the route before the first side effect: healthy cloud uses REST, unavailable cloud uses BLE only with a live final device and valid READY session, and final-device loss blocks the sale. An ambiguous REST result becomes `UNKNOWN/RECONCILE` and is never retried over BLE. Offline commands carry a bounded expiry and SHA-256 of canonical CBOR payload.
- BeeMiniPOS now has a native Android/iOS GATT client based on `react-native-ble-plx`: platform permission handling, service-filtered scan, connect/discovery, MTU negotiation, notifications, authenticated HELLO→CHALLENGE→AUTH_PROOF→READY, encrypted command chunks, ACK/NACK flow and correlated terminal result. Portable Noble cryptography removes WebCrypto/base64 browser dependencies while preserving the same X25519/HKDF/AES-GCM protocol on Web and native.
- Web BLE now offers a correlated send-and-wait primitive with bounded timeout, exact message/operation binding, duplicate suppression and waiter cleanup. The reusable fiscal checkout adapter maps both REST and BLE terminal results into the same COMPLETED/ACCEPTED/UNKNOWN/BLOCKED model. A short-lived encrypted `DEVICE_PROBE` traverses MiniPOS→Edge→final ФУ without allocating УНП/operation sequence; only its fresh READY result may authorize local fallback.
- BLE revoke is atomically persisted with a `ble.session.revoked` outbox event. Edge has a restart-durable SQLite revocation cache with expiry GC, and its handshake rejects cached revoked tickets before emitting CHALLENGE.
- Edge is now an executable, container-buildable process with guarded DEV/PROD configuration, graceful shutdown, periodic bounded sync draining, health endpoint and an HMAC-authenticated Fiscal control webhook. BLE revocation is acknowledged only after durable SQLite commit and survives process restart; invalid/tampered/non-revocation events fail closed.
- The Edge executable now composes authority, journal, runtime and a configured final-device adapter. A bearer-protected DEV/HIL transport executes canonical commands through the real durable path, enforces tenant/register/device/fencing binding, exposes the independent final-device probe, and is absent in PROD. Process restart smoke proves a completed command retains the same sequences/result with exactly two journal records and no second device execution. `simulator` hard-fails in PROD; unapproved tracks use an always-unreachable `unsupported` adapter.
- Edge has a transport-neutral GATT command processor that decrypts/reassembles frames, strictly validates CBOR envelope/hash/expiry, invokes the durable runtime and returns encrypted result frames correlated by message ID. Nested CBOR maps are normalized to string-keyed objects before driver JSON serialization; an encrypted end-to-end test covers the complete path and proves expired commands never execute.
- The GATT processor binds every command and probe to the authenticated ticket tenant/register/edge/fencing tuple before runtime invocation; cross-tenant or stale-fence commands never reach the device. BLE session `binding_version` exposes the actual fencing token through the existing OpenAPI field.
- Connectivity probes are durable and tenant-isolated; readiness/probe returns `BLOCK` when the final fiscal device is unavailable. Simulator unknown-after-send creates `UNKNOWN` and cannot be blindly retried.
- Readiness is no longer driver/simulator-only: the public device endpoint first requires the requested device to belong to the authenticated tenant, be `ACTIVE` and have fiscal capability. Connectivity probes additionally require an ACTIVE register with an ACTIVE bound fiscal device before probing the driver. Unknown, foreign, DRAFT, BLOCKED and unbound devices return `BLOCK` even when the simulator is healthy.
- Core sale/operation JSON names, sale states, payment IDs and required action arrays are aligned with the canonical OpenAPI contract.
- Authenticated tenant-scoped Fiscal sales/operations and MiniPOS catalog/order filtering. Protected endpoints require the OpenAPI OAuth authorization scope `fiscal.base`. Public APIs enforce a centralized fail-closed RBAC policy: cashier sale/shift access, supervisor fiscal reports/reversal/export, administrator configuration/provisioning/webhooks, auditor read-only access and service-only Edge sync/diagnostics. Missing scopes and unknown roles are rejected.
- Production authentication now requires OIDC RS256/JWKS and rejects configured HMAC, missing issuer/audience/JWKS or non-HTTPS trust endpoints. Verification pins `alg=RS256`, selects a non-empty `kid`, validates issuer/audience/expiry/tenant/roles/scope, bounds JWKS downloads, caches keys and refreshes immediately for a rotated unknown `kid`; tests cover rotation, multiple audiences, wrong issuer/audience and attacker signatures. HS256 remains DEV/E2E-only.
- Production MiniPOS→Fiscal authentication now uses OAuth 2.0 client credentials over HTTPS instead of a static service token. The provider uses HTTP Basic client authentication, explicit scope/audience, validates Bearer token responses and bounded expiry, single-flights concurrent acquisition, caches with a 30-second safety window, invalidates on 401 and retries the same idempotent Fiscal request exactly once. Static `FISCAL_AUTH_TOKEN` is DEV-only and rejected in PROD.
- Both public APIs enforce an in-process tenant-keyed (authenticated) or source-IP-keyed (DEV) fixed-window limit, return HTTP 429 plus `Retry-After`, and leave health and the separately HMAC-authenticated internal webhook path outside that public-client budget.
- Typed Fiscal and MiniPOS schemas define LOGIN non-owner/no-`BYPASSRLS` roles, fail-closed transaction-local tenant context functions and `FORCE ROW LEVEL SECURITY` policies, including ownership-through-parent policies for line/event/order descendants. Each backend opens a physically separate non-owner reader pool; PROD rejects missing RLS credentials or reuse of the writer database user. Real PostgreSQL tests authenticate with those reader credentials and prove foreign-tenant invisibility and rejection of cross-tenant inserts.
- Fiscal sales/operations/shifts/resources plus tenant-bound UNP sequence, API replay, webhook outbox, BLE sessions, connectivity probes, Edge pending commands, immutable binary artifacts with SHA-256, Edge sync acknowledgements and audit events, and MiniPOS products/employees/shifts/orders/single-site configuration/API replay/webhook inbox/checkout result+hash checkpoints have authoritative typed projections with explicit tenant keys, domain constraints, indexes and FORCE RLS. UNP allocation uses a tenant+register key and atomically absorbs a pre-tenant register sequence on its first authenticated allocation without reusing a number; tenantless allocation remains only in the explicit in-memory DEV fallback. Authenticated Edge sync keys its journal cursor by tenant+edge, rejects a command whose embedded tenant differs from the JWT tenant and persists its acknowledgement in the same transaction as materialized sale/operation/artifact/outbox/pending changes. Artifact keys are tenant+id; pre-tenant artifacts migrate on first authenticated read and roll back to the legacy key if persistence fails. Checkout result and its 64-character request hash are projected in the same serializable repository transaction; malformed hashes roll back both representations. Every projection upsert/delete assumes the non-owner role and transaction-local tenant context inside the same serializable transaction as compatibility state. Integration tests prove normalization, foreign-tenant invisibility, cross-tenant reassignment rejection and atomic rollback of both representations. MiniPOS requires webhook `tenant_id`, persists it in the inbox and refuses to link an event to a foreign-tenant order. Legacy pre-typed MiniPOS configuration rows remain compatibility-readable and are normalized on the next API save.
- Persistent single-entity and collection hot-path reads for Fiscal sale/operation/shift/admin resources and MiniPOS product/employee/shift/order execute against typed tables through the separate reader identity in read-only transactions with transaction-local tenant/organization context. RLS returns no row for a foreign tenant; real PostgreSQL tests cover allowed and denied reads. DEV without persistent storage retains an explicit in-memory fallback.
- Fiscal authenticated orchestration now also reads BLE sessions, connectivity probes, Edge pending commands, Edge journal acknowledgements, replay records, immutable artifacts and audit events from their typed RLS tables. The cross-tenant outbox dispatcher uses a separate explicitly system-only typed reader through the writer identity; that reader is not exposed to public handlers. Sale/payment/cancel/reversal/reconciliation/receipt/shift mutation endpoints perform their authoritative precondition read with the request tenant rather than re-reading an unscoped compatibility map. Tests prove a foreign tenant cannot fall back to a stale in-memory sale.
- MiniPOS API replay and checkout result/hash checkpoints are read from typed RLS tables. A repeated checkout resolves the typed checkpoint and validates the original request hash before any Fiscal call; a half-written/invalid checkpoint or reader failure fails closed. Product/employee/order/shift mutations validate the tenant-owned typed version/state before changing the compatibility coordinator, and sales reporting reads typed tenant orders. Real PostgreSQL tests cover permitted and foreign replay/webhook/checkout reads.
- Both PostgreSQL row stores now expose an atomic repeatable-read snapshot plus monotonic generation token and compare-and-swap save. Two backend instances loaded at the same generation cannot overwrite one another: the stale writer receives a generation conflict before any compatibility or typed row changes, then reloads the authoritative snapshot/generation into its in-process coordinator. Unit/race tests prove failed stale mutations disappear from memory; real PostgreSQL tests use two independent connections and prove the first committed state survives unchanged. This closes lost-update risk while aggregate-specific write commands are introduced incrementally.
- Runtime persistence now keeps the last confirmed snapshot only as a delta baseline. Each successful mutation sends explicit changed/deleted state rows to `SaveDeltaVersioned`; PostgreSQL locks/checks generation first, then upserts/deletes only those rows and their typed projections. It no longer queries all compatibility rows or derives database-wide stale deletes on the normal write path. Real PostgreSQL tests inject an unrelated row absent from the coordinator baseline and prove a delta mutation preserves it, while coordinator tests assert the delta interface—not full `SaveVersioned`—is used.
- The PostgreSQL migration path now has an executable legacy-to-typed drill for both products: a real legacy `runtime_snapshots` payload is migrated atomically, then the test verifies both the reconstructed row state and its tenant-bound authoritative typed sale/product projection. Compatibility removal is still gated on a typed-only restart and rollback drill.
- Both products now activate typed-only durable mode only after an exact-payload coverage guard succeeds. Main constrained aggregates retain their exact domain payload alongside indexed columns; technical aggregates are reconstructed from payload or a canonical typed value. Mode 2 stops reading and writing compatibility rows, preserves MiniPOS sequence in versioned meta and rebuilds the coordinator cache solely from typed tables. PostgreSQL tests corrupt the dormant compatibility copy and prove restart still returns typed state; typed constraint failures roll back both data and generation. Physical deletion remains a separately approved destructive migration after the rollback window.
- Stage 21 fault regression is now an executable release gate with 20 cases across device, BLE, authority, restart, sync, storage, retention, clock and routing. New tests prove partial/signed-wrong ACK, backend rollback, malformed ACK and transport loss never acknowledge data and that the identical idempotent batch recovers; authority handback requires strictly increasing fencing/ranges; storage 70/85 remains operational while 95/100 blocks; clock rollback cannot shorten retention.
- Stage 23 security regression is gated by 18 executable cases across authentication, authorization, webhook, SSRF, replay, revoke, rate limiting, idempotency, production guards and secret rotation. A discovered weakness accepting a raw JWT without the `Bearer` scheme was fixed in both backends; MiniPOS webhook auth exemption is now exact-path only. Sensitive payment-data scanning remains clean.
- BG software evidence was strengthened: totals now run a 120-case EUR quantity/price/split property matrix; UNP runs a 128-way concurrent uniqueness/monotonicity/tenant-scope test; reversal persists immutable reason and validated original fiscal reference and rejects a second reversal; three-month retention explicitly covers clock rollback and contiguous safe GC. Vendor/SD/legal portions remain external gates rather than simulated PASS.
- Separate Fiscal and MiniPOS PostgreSQL migration baselines.
- Two isolated Compose projects with distinct PostgreSQL, private/ingress networks, Caddy ingress and explicit DEV/PROD overlays.
- Both backends tolerate transient Docker DNS/database readiness races with a bounded 30-second startup retry; permanent errors still terminate startup. DEV Caddy listens on `:80` so host and cross-compose routes work, while PROD remains bound to the explicitly required HTTPS site name.
- Touch MiniPOS and fiscal administration Expo UI for Android/iOS/Web. BeeFiscalApp now loads registered devices, filtered operations, persisted fiscal reports, effective country/tax policy and readiness/connectivity data from the public API; device and report rows are no longer hard-coded or locally simulated.
- BeeMiniPOS now exposes the required cross-platform IDs for transport, BLE, cash/card payment, unknown outcome and reconciliation. Interactive controls meet the 48dp minimum and primary payment/reconciliation CTAs meet 56dp. Once fiscal routing can have side effects, the UI freezes the order, disables both payment methods, displays a prominent no-retry reconciliation panel and performs only `GET /orders/{id}` polling; it clears the gate only for terminal `COMPLETED`, `FAILED` or `CANCELLED` state and never invokes checkout from reconciliation.
- Stage 22 now includes a machine-checked 13-case Android/iOS/Web UI acceptance matrix for both Expo apps. It gates MiniPOS touch/accessibility/test IDs, UNKNOWN no-retry behavior, final-device separation, Web BLE user gesture and public-API boundaries; FiscalApp public data, hop-by-hop readiness, reports, administration editors and accessible register filtering; and all three build paths. This is source/build evidence only and does not replace physical-device or screen-reader HIL.
- BeeFiscalApp parity now includes accessible tab semantics/selection, live Core and transport status, labelled device selection, UNKNOWN operation marker, report actions and end-to-end readiness probe. Its navigation/input/action targets are at least 48dp and the primary probe is 56dp; the same executable UI matrix prevents regression of these anchors.
- BeeFiscalApp now includes a public-API-backed administration workspace for locations, registers, Fiscal operators, DEV fiscal devices and register-to-device bindings. Existing resources are selectable and editable with optimistic `If-Match` versions; new resources use POST, bindings use the canonical endpoint, and success/error/loading/empty states are announced through accessible UI. No editor writes local authoritative records.
- Both Expo UIs now call backend APIs; MiniPOS no longer simulates a successful receipt and includes product, employee, location/workstation editors, active-employee selection, quantity controls and its commercial sales report.
- Kotlin stable driver interface and a buildable Daisy SMART S Android STUB app with deterministic fiscal/card/barcode/error scenarios, activated-terminal enforcement, debug-only plus PROD constructor guards and contract tests. Debug and unsigned release APKs compile; the release artifact cannot instantiate the STUB because `BuildConfig.DEBUG` is false.
- The C++ IoT abstraction classifies all 88 Daisy and 73 Datecs commands and implements validated vendor payload builders/golden response parsers for the execution-critical receipt path: open, storno, item/programmed PLU, payment, close/cancel mapping, X/Z report, EUR cash movement, frame status and receipt-status reconciliation. Datecs semantic coverage also includes commands 43/45/50/51/58/62/64/65/68/71/86/87/88/89/90/94/95/99/100/103/105/110/111/112/115/116/123/124/125/126/135 for original-document/invoice/УНП-bound reversal, PC mode control, VAT rates and daily/operator totals, subtotal/discount, clock and fiscal-memory state/capacity/self-test/binary/structured records and date/Z-range reports, item groups/departments, diagnostics, tax identity, error lookup, current-receipt state, operator/PLU reports, typed 13-field NRA delivery status, BC-50 currency conversion, all device-information options, typed receipt-period search, bounded EJ text/base64/CSV transport and modem identity/signal. Command 135 remains `GetDiagnosticInfo`; GSM availability is not NRA-delivery evidence, which now comes only from command 71. A machine-readable coverage gate reports 8 Daisy/40 Datecs semantic commands, covering every Datecs `Supported` command, and keeps hardware state explicitly `DOCUMENTED_NOT_HARDWARE_VERIFIED`; optional/privileged/excluded commands, HIL and full CSV-column meaning are not promoted implicitly.
- C++ protocol abstraction and exhaustive safe-fail command registries for Daisy and Datecs. Every one of the 88/73 documented IDs produces a disposition/retry/canonical classification; unknown IDs and conditional payment commands fail closed.
- Portable Daisy/Datecs frame encoders/parsers with BCC validation and compiled inventories of all 88 Daisy / 73 Datecs documented command codes.
- Edge measures allocated SQLite pages against `EDGE_STORAGE_QUOTA_BYTES`, exposes authenticated DEV/HIL storage telemetry, classifies 70/85/95/100% as WARNING/HIGH/CRITICAL/FULL and blocks a new fiscal command at 95% before probe, sequence allocation, journal append or device execution. Existing durable results remain replayable.
- A machine-checked release evidence generator inventories Go, npm and container dependencies as CycloneDX 1.6, hashes contracts/migrations/deployment baselines and emits a release manifest/checksum set. Until a release vulnerability report and cryptographic signatures exist, verification requires `UNSIGNED_NO_GO` and `PROD_NO_GO`; evidence generation cannot silently promote the build.

## Evidence currently passing

- Go unit/API/integration/race tests for three Go modules.
- Both row-stores have opt-in real PostgreSQL integration suites covering multi-entity rows, close/reopen recovery, legacy snapshot migration and the authoritative-empty-state corner case.
- TypeScript checks for both Expo apps.
- All four DEV/PROD Compose configurations render with required variables.
- Manual local E2E MiniPOS→Fiscal simulator reached `COMPLETED` with fiscal sale/operation references.
- Clean-room container smokes proved MiniPOS and Fiscal data survive backend restart through Caddy/PostgreSQL; the temporary containers/volumes were removed afterwards.
- Automated two-project Compose E2E passes through both Caddy entry points using a short-lived DEV/E2E HS256 bearer session with an explicit tenant, `ADMIN` role and `fiscal.base` scope; the same authenticated tenant token is used by MiniPOS for its public Fiscal API calls, so FORCE RLS is exercised with real claims rather than an empty DEV tenant. The scenario covers autonomous configuration/catalog/employee/shift/order creation, MiniPOS→Fiscal EUR cash fiscalization, completed-state verification, both backend restarts with recovery, and MiniPOS database autonomy while Fiscal PostgreSQL is stopped. Its fault branch stops Fiscal before checkout, requires HTTP 502 plus a durably persisted `UNKNOWN` order, then proves replay of the same idempotency key after recovery remains the same non-success response and does not silently retry the fiscal sale. The test also creates custom-format backups of both isolated PostgreSQL databases, restores them to fresh databases, checks restored state rows, and enforces an MVP RTO below 120 seconds. HTTP assertions run from pinned `curlimages/curl:8.12.1` through a disposable test-only ingress containing both Caddy containers, both backends and the test client; every curl has bounded connect/total time, including the health loop, so a Docker network blackhole cannot bypass the harness timeout. Neither PostgreSQL is attached to ingress. The E2E harness discovers container IPs and injects the supported `*_DB_HOST`/`*_UPSTREAM` overrides to avoid reproducible Docker Desktop embedded-DNS and dynamic-network-routing defects; normal DEV/PROD defaults remain Compose service names and backend isolation. MiniPOS still invokes Fiscal only through Fiscal Caddy/public API, and Caddy remains each deployed system's sole external entry point. The script removes only its PID-scoped projects/network/volumes and prints response bodies plus step-scoped logs on failure.
- A clean isolated rerun on 2026-08-09 passed the complete two-Compose scenario after an earlier Docker Desktop bridge startup failure; the passing rerun used fresh PID-scoped networks/volumes and required no application or harness retry that could conceal a deterministic service failure.
- The two-Compose harness now provisions location, register, DEV fiscal device through the mandatory `DRAFT → PENDING_SERVICE_ACTIVATION → ACTIVE` transitions, binding and active Fiscal operator through the authenticated Fiscal public API, then writes the returned register UUID into autonomous MiniPOS configuration. Its successful sale therefore proves the same lifecycle/registry/operator-aware execution gate used outside tests rather than relying on synthetic unregistered identifiers.
- Both Expo Web exports and BeeMiniPOS Android/iOS bundles succeed. A real Android debug APK (`assembleDebug`, 179 tasks) and an unsigned universal iOS Simulator app (`xcodebuild`, 83 targets) compile successfully with the native BLE module linked. Signed release/store builds and physical-device HIL remain release gates.
- Daisy SMART S STUB contract tests pass and both debug and unsigned release APK variants build with AGP 8.8.2/Kotlin 2.0.21; static/runtime guards prevent release/PROD activation.
- Contract gate resolves all local `$ref` values, rejects duplicate `operationId`, verifies router registration for all 73 canonical plus 8 runtime OpenAPI operations, and parses AsyncAPI 3.0.0.
- Both Expo dependency trees have no critical/high `npm audit` findings after pinning PostCSS 8.5.23. Ten moderate Expo 53 build-tool findings remain tracked pending a controlled SDK upgrade; npm's incompatible Expo 46 downgrade is not applied.
- Three consecutive Stage 24 full regression cycles pass after the Docker test-network fix, including the tenant-bound artifact and Edge-sync ACK cutover: race/unit suites, TypeScript, 73+8 OpenAPI surface/AsyncAPI, sensitive-data and CycloneDX evidence gates, Web/Android/iOS bundles, Daisy STUB debug/release builds, C++ protocol tests, four Compose renders, real PostgreSQL RLS/rollback tests and the complete two-Compose E2E. The harness does not auto-restart services while waiting for health, so deterministic failures are not converted into flaky passes.
- On 2026-08-09, after the Stage 21 fault matrix, Stage 23 security matrix, typed-only persistence and strengthened BG evidence changes, two additional consecutive `make full-regression` runs passed on an unchanged tree. Both runs included PostgreSQL integration and the complete two-Compose sale/outage/restart/backup/restore/autonomy scenario through both Caddy entry points.
- On 2026-08-09 the full regression also passed after the registry-aware execution cutover: 19-case security matrix, race suites, PostgreSQL typed/RLS tests, platform bundles, device/protocol builds and public-API-provisioned two-Compose E2E all completed successfully.

## Deliberately gated, not falsely implemented

- Real Daisy SMART S SDK, Daisy Compact USB/HIL, DP-150 EUR HIL, BlueCash/BluePad/acquirer, production BIM/service/legal evidence.
- These capabilities remain `EXCLUDED_FROM_MVP` or `DOCUMENTED_NOT_APPROVED` and cannot be enabled in PROD.

## Remaining before functional-complete freeze

- BG-020 is blocked at the canonical contract boundary: the regulatory matrix requires separate BGN and EUR files for a request crossing 2025-12-31/2026-01-01, but `ComplianceExport` currently exposes one `artifact` and constrains `official_currency` to EUR. Because every public field must be OpenAPI-described, implementation must wait for an additive canonical schema revision defining per-period artifact manifests and currency. No undocumented response shape is emitted.
- Typed tables are now the sole active durable representation in storage mode 2 and exact CAS delta/restart/rollback are proven. Dormant compatibility rows are retained only as a non-runtime rollback copy pending an explicitly approved destructive migration. Removal of in-process coordinator maps is a post-MVP internal refactor, not a canonical MVP gate. Historical `devices` has no runtime reader or writer and is formally excluded from MVP.
- RBAC/RLS and software provisioning enforcement are complete for the MVP boundary: central JWT/RBAC, FORCE RLS reader isolation, fail-closed device transitions, ACTIVE-only binding, active-register requirement, PROD activation evidence references and provisioning eligibility are test-covered. Actual application 24, approved-type and authorized-service documents remain external deployment evidence.
- Hardware GATT server plus USB/UART adapter wiring on the Edge devices remains. BeeMiniPOS Web and native clients activate a public-API BLE session from an explicit user gesture/permission grant, perform cloud/readiness or encrypted final-device probe before checkout, freeze REST/BLE/BLOCK routing, wait for the correlated fiscal result and never report success for UNKNOWN/BLOCKED. Chrome Web Bluetooth and native Android/iOS GATT clients plus Edge cryptography/CBOR/framing/flow/runtime/revocation/sync primitives are wired at the application layer; physical hardware HIL is still required.
- Remaining Fiscal surface: full typed response validation and generated contract compatibility evidence.
- Complete per-command encoder/parser semantics and golden/HIL vectors beyond the now executable core receipt/report/cash/reconciliation mappings.
- BeeFiscalApp administration editors and software-verifiable exceptional/empty/loading accessibility states are implemented; physical assistive-technology interaction evidence remains part of the UI HIL gate.
- Compliance gaps that remain software-addressable are tracked row-by-row in `contracts/bg-requirements-trace.json`; hardware, vendor, payment-acquirer and legal/XSD evidence remains explicitly external and cannot be promoted by simulator tests.
