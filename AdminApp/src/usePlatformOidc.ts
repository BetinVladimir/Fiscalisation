import {useEffect,useMemo,useState} from "react";
import * as AuthSession from "expo-auth-session";
import * as WebBrowser from "expo-web-browser";
WebBrowser.maybeCompleteAuthSession();
const scopes=["openid","profile","beefiscal.platform"];
export function usePlatformOidc(){const issuer=(process.env.EXPO_PUBLIC_PLATFORM_OIDC_ISSUER||"").replace(/\/$/,"");const clientId=process.env.EXPO_PUBLIC_PLATFORM_OIDC_CLIENT_ID||"";const configured=issuer.startsWith("https://")&&!!clientId;const discovery=AuthSession.useAutoDiscovery(configured?issuer:"");const redirectUri=useMemo(()=>AuthSession.makeRedirectUri({scheme:"beefiscalplatformadmin",path:"oauth/callback"}),[]);const [request,response,promptAsync]=AuthSession.useAuthRequest({clientId,redirectUri,responseType:AuthSession.ResponseType.Code,scopes,usePKCE:true},configured?discovery:null);const[token,setToken]=useState("");const[error,setError]=useState("");useEffect(()=>{if(response?.type!=="success")return;if(!request?.codeVerifier||!discovery){setError("OIDC_PKCE_STATE_MISSING");return}void AuthSession.exchangeCodeAsync({clientId,code:response.params.code,redirectUri,extraParams:{code_verifier:request.codeVerifier}},discovery).then(x=>{if(!x.accessToken)throw new Error("OIDC_ACCESS_TOKEN_MISSING");setToken(x.accessToken);setError("")}).catch(e=>setError(String(e)))},[response,request,discovery]);return{configured,ready:configured&&!!request&&!!discovery,accessToken:token,error,login:()=>promptAsync(),logout:()=>setToken("")}}
