# Contract lock

Canonical public contracts are maintained with the regulatory documentation and are consumed by this implementation without redefining business schemas:

- `../../BeeloyBackend/docs/Fiscal/api/openapi-public-v1.yaml` — SHA-256 `5aeacae5be26b5f8c6cb19b48e6725bb751dd7ad2ce6bee2773044964cdce203`;
- `../../BeeloyBackend/docs/Fiscal/events/asyncapi-device-v1.yaml` — SHA-256 `16c9ebb272c272189d08843739b1e6ca439aff16844e7d307cab835212cfe797`.
- `openapi-runtime-v1.yaml` — SHA-256 `9c299e5f932d9d860049caa9b92a4cbc487550e490b392c0c854b91959de3bf8`; reviewed implementation contract adding aggregate `SALE_FINALIZE`, including its persisted client operation and receipt-session identities and the actual BLE aggregate intent fields (`tenant/register/edge/binding`, immutable items and ordered payments), two-phase `CompositeDeviceBinding` creation/apply acknowledgement with exact adapter/fiscal/payment generation, the exact persisted operation projection, the BG_SUPTO_FULL lifecycle, signed workstation readiness, protected loopback Local Compliance Intent surface including Datecs REVERSAL source-document requisites, MQTT `REVERSAL`/`REVERSED` envelopes, tenantless proof-of-possession SmartDevice bootstrap/challenge/credential/lookup/confirm/disconnect endpoints, MiniPOS fiscal enrollment start/verify contracts, the corrected MiniPOS reversal response, ESP32-generated P-256 identity signatures in fixed IEEE P1363 `r || s` form, per-device operational keys and fenced УНП range, legacy BlueCash BLE activation-token DEV compatibility, structured timestamped `BeeFiscal-Signature` webhook HMAC and mandatory body-bound `BeeFiscal-Event-Id`, separate compatible general idempotency and MiniPOS-generated UUID `ClientOperationId` profiles, plus the existing BG-020 exports, MiniPOS lifecycle and DEV/HIL Edge surfaces.
- `openapi-corrections-v1.yaml` — SHA-256 `b46b930eb09fcc1a284238b546e92b8bf663ba2cd27956a5504046f2ff6fca1a`; OpenAPI Overlay 1.0 with a machine-gated inventory of 22 corrections, including the durable workstation-session logout endpoint and complete Annex audit filters, plus the fiscal line discount/device receipt snapshot, historical export filters, BLE binding/security corrections and server-owned request-schema fixes. Generated clients and request/response validators use the effective overlaid contract while the locked canonical bytes remain unchanged.

The lock is intentionally explicit: changing any source requires a reviewed contract diff, regenerated clients and an update of these hashes. REST, webhook and BLE payloads must reference the canonical schemas rather than introduce transport-specific business models. The runtime completion document may only close implementation-surface gaps; it cannot redefine a business schema already owned by the regulatory contract. The corrections overlay is checked against an ordered allowlist of exact JSONPath targets, required-field inventories and the still-present locked canonical defects.

`contracts/generated/openapi-public-v1.d.ts` and `openapi-runtime-v1.d.ts` are reproducible derived artifacts generated by pinned `openapi-typescript` 7.13.0. `make generate-openapi` refreshes them; `make contract-test` regenerates into an isolated temporary directory, requires byte-for-byte equality, compiles typed `openapi-fetch` client factories and verifies that every canonical/runtime `operationId` exists in the generated surface. Generated files never override the locked YAML sources.
