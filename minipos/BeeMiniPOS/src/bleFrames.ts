import{hash256}from"./portableCrypto.ts";
const MAGIC=[0x42,0x46,0x46,0x31],HEADER=57,MAX=8192;
export function uuidBytes(id:string){const hex=id.replaceAll("-","");if(!/^[0-9a-fA-F]{32}$/.test(hex))throw new Error("BLE_MESSAGE_ID_INVALID");return Uint8Array.from(hex.match(/../g)!.map(v=>parseInt(v,16)))}
export function uuidString(v:Uint8Array){const h=[...v].map(x=>x.toString(16).padStart(2,"0")).join("");return`${h.slice(0,8)}-${h.slice(8,12)}-${h.slice(12,16)}-${h.slice(16,20)}-${h.slice(20)}`}
export async function frameMessage(payload:Uint8Array,messageId:string,attMtu=185){if(!payload.length||payload.length>MAX)throw new Error("BLE_INTENT_TOO_LARGE");const capacity=attMtu-3-HEADER;if(capacity<1)throw new Error("BLE_MTU_TOO_SMALL");const id=uuidBytes(messageId),digest=hash256(payload),out:Uint8Array[]=[];for(let offset=0;offset<payload.length;offset+=capacity){const body=payload.slice(offset,offset+capacity),frame=new Uint8Array(HEADER+body.length),view=new DataView(frame.buffer);frame.set(MAGIC);frame.set(id,4);view.setUint16(20,payload.length);view.setUint16(22,offset);frame[24]=offset+body.length===payload.length?1:0;frame.set(digest,25);frame.set(body,HEADER);out.push(frame)}return out}
export class FrameAssembler{private states=new Map<string,{total:number;digest:Uint8Array;next:number;parts:Uint8Array[]}>();async accept(frame:Uint8Array){if(frame.length<=HEADER||!MAGIC.every((v,i)=>frame[i]===v))throw new Error("BLE_FRAME_INVALID");const id=uuidString(frame.slice(4,20)),view=new DataView(frame.buffer,frame.byteOffset,frame.byteLength),total=view.getUint16(20),offset=view.getUint16(22),final=frame[24]===1,digest=frame.slice(25,57),body=frame.slice(57);if(!total||total>MAX||offset+body.length>total)throw new Error("BLE_FRAME_BOUNDS");let state=this.states.get(id);if(!state){if(offset!==0)throw new Error("BLE_FRAME_ORDER");state={total,digest,next:0,parts:[]};this.states.set(id,state)}if(state.total!==total||state.next!==offset||!state.digest.every((v,i)=>v===digest[i])){this.states.delete(id);throw new Error("BLE_FRAME_CONFLICT")}state.parts.push(body);state.next+=body.length;if(!final)return null;if(state.next!==total){this.states.delete(id);throw new Error("BLE_FRAME_INCOMPLETE")}const payload=new Uint8Array(total);let p=0;for(const part of state.parts){payload.set(part,p);p+=part.length}this.states.delete(id);const actual=hash256(payload);if(!actual.every((v,i)=>v===digest[i]))throw new Error("BLE_FRAME_DIGEST");return{id,payload}}
}
