# MVP completion audit

Дата среза: 2026-08-14. Статус цели: **SOFTWARE_COMPLETE_HIL_PENDING / PROD NO-GO**.

Этот документ не подменяет canonical roadmap. Он связывает фактическую реализацию в данном репозитории с критериями этапов 20–25 и запрещает считать simulator evidence доказательством hardware/legal readiness.

Полное соответствие этапам 1–25 дополнительно зафиксировано в `contracts/roadmap-stage-acceptance.json` и проверяется `make roadmap-acceptance-test`. Реестр требует существования каждого evidence artifact и Make target, запрещает gap у `SOFTWARE_PASS`, требует явный gap у внешних стадий и сохраняет Stage 25 в `PENDING_EXTERNAL_ACCEPTANCE`, пока acceptance record не подписан реальными approvers.

Stage 1 дополнительно проверяется `make governance-test`: два независимых product/database/Compose/Caddy ownership boundary, десять обязательных module owner roles, единая identifier policy и 14 P0 decision records. Внешний P0 не может пройти gate без существующего evidence artifact и `production_blocked=true`.

## Доказано текущим кодом

| Область | Статус | Авторитетное evidence |
|---|---|---|
| Fiscal и MiniPOS public API | PASS для generated compatibility и runtime request/response enforcement на реализованном MVP surface | 74 effective canonical + 64 runtime operations; 138 generated request and 138 success contracts; byte-stable TypeScript surfaces and all-operation typed client compilation. Shift recovery is operator/app-session authorized; Z-close reconciliation is a separate documented operation and cannot repeat Z when an operation ID exists. Undocumented success and malformed Problem responses fail closed |
| Автономность MiniPOS | PASS | отдельная PostgreSQL/Compose; E2E читает MiniPOS при остановленной Fiscal PostgreSQL |
| EUR cash fiscalization | PASS на simulator | two-Compose sale через оба Caddy; original operation/receipt reference is returned, stored in typed MiniPOS order and checkout evidence, kept distinct from storno reference, and verified again after restart and PostgreSQL restore |
| Ambiguous outcome | PASS | E2E требует 502 + durable `UNKNOWN`; replay того же key не повторяет fiscal sale |
| REST/BLE route freeze | PASS на software transport | MiniPOS tests: REST preferred, BLE only after READY/live probe, no REST→BLE retry after ambiguous send |
| Registry-aware final-device gate | PASS на software transport | readiness requires tenant-owned ACTIVE fiscal device; connectivity requires ACTIVE register binding; unknown/foreign/DRAFT/BLOCKED/unbound fixtures all return BLOCK before driver probe |
| BLE authority issuance | PASS на software transport | issue requires tenant-owned ACTIVE register with ACTIVE fiscal/smart-device binding; refresh is rejected after the bound device becomes BLOCKED; cross-tenant and unknown-register cases are race-tested |
| Registry-aware fiscal execution | PASS на central software path | sale, shift, payment, reversal, cash/report execution require tenant-owned ACTIVE register and ACTIVE fiscal-device binding before driver invocation; counting-driver negative test plus public-API-provisioned two-Compose sale prove fail-closed and success paths |
| Operator authority | PASS на central software path | sale/shift require a same-tenant operator within `active_from`/`active_to`; future and expired fixtures fail, while public-API-provisioned operator completes the two-Compose sale |
| Edge durability | PASS на SQLite implementation | WAL/FULL, durable-before-device, restart replay, signed sync/ACK, three-month ACK-gated GC |
| Edge storage pressure | PASS на software storage | 70/85/95/100 states; authenticated status API; ≥95% blocks before sequence/journal/device |
| Authn/authz and public flow control | PASS для software MVP | PROD OIDC RS256/JWKS with issuer/audience/key-rotation tests; DEV-only HMAC; scope/RBAC/cross-tenant service checks; tenant/IP rate limit with 429/Retry-After |
| Webhook security | PASS | canonical `t,kid,v1`, 300 s window, constant-time HMAC, SSRF/redirect controls, rotation/410/retry tests |
| Backup/restore | PASS для pilot drill | custom-format dump/restore обеих БД, restored-row assertion, RTO <120 s |
| Supply-chain inventory/signature | PASS как software NO-GO evidence | CycloneDX 1.6, transitive file hashes and manifest; optional Ed25519 signature requires an independently trusted public key and rejects byte tampering. Unsigned evidence remains `UNSIGNED_NO_GO`; signed-but-unscanned/external-gated evidence remains `PROD_NO_GO` |
| Signed OTA policy | PASS для hardware-independent Edge control plane | Ed25519 target-bound manifest and artifact verification before stage/boot; monotonic anti-replay; durable A/B health confirmation; approved-floor rollback or `RECOVERY_REQUIRED`. Secure boot/flash/recovery/vendor HIL remains external |
| Daisy SMART S | PASS только STUB boundary | debug/release builds and tests; release/PROD instantiation hard-disabled |
| Daisy/Datecs protocols | PASS для inventory/frame baseline | 88/73 classification, frame/BCC compilation and negative unknown-command behavior |
| BG-020 currency-period export | PASS для software requirement | additive OpenAPI operations create/get/download a half-open periodized export; cross-boundary requests produce separate BGN/EUR artifacts, boundary rows occur exactly once, tenant isolation/restart and two-Compose PostgreSQL restore pass |
| Regression stability | PASS текущего software slice | unified regression после MiniPOS BLE deployment-binding hardening завершился с exit 0: Go race/vet, 92/92 contracts, fault 70, security 56, UI 42, Android/iOS/Web обеих Expo apps, Daisy SMART STUB, C++/IoT, clean PostgreSQL migrations/typed/RLS и независимый two-Compose/Caddy outage/restart/backup-restore E2E. Identity authority, immutable reporting, principal-bound idempotency, resource business-key/CAS и representation-header replay остаются покрыты. |

## Незакрытая software implementation

| Gap | Почему completion не доказан | Следующее доказательство |
|---|---|---|
| PostgreSQL typed persistence + RLS | PASS для canonical MVP: storage mode 2 использует typed tables как единственное активное durable representation; exact CAS delta, typed-only restart и rollback доказаны на PostgreSQL. Compatibility corruption игнорируется. Legacy `devices` исключён | удаление in-process coordinator maps является post-MVP refactor; физическое удаление пассивных compatibility rows — отдельная подтверждаемая destructive migration после rollback window |
| Production OIDC/OAuth 2.1 | PASS на уровне software: inbound RS256/JWKS cache+rotation/issuer/audience; outbound MiniPOS→Fiscal client credentials с cache, refresh и однократным 401 retry; HMAC/static service token запрещены в PROD | для deployment GO требуется evidence принятого внешнего IdP/client registration |
| MiniPOS operator identity/session | PASS для software enforcement: both Expo apps use OIDC Authorization Code + PKCE in PROD; access/rotating refresh credentials remain memory-only and refresh before expiry, while absent/failed refresh clears authority fail closed; ADMIN-only immutable issuer+subject binding resolves exactly one active employee; raw subjects/tokens are not persisted; session is bound to app instance; cross-employee/app-instance actions fail; logout stores only a SHA-256 credential fingerprint and revocation survives restart under RLS | deployment GO still requires an organization IdP configured for passwordless/passkey policy plus real issuer/client registration and IdP-side login/refresh evidence |
| Device provisioning enforcement | PASS на уровне software: creation is DRAFT-only; lifecycle transitions, ACTIVE-only register binding, active-register guard, PROD evidence references and provisioning eligibility are fail-closed and race/E2E-tested | application 24, approved-type registry and authorized-service dossier remain external deployment evidence |
| Full card orchestration | PASS для software registry/STUB boundary: explicit CARD never falls back to cash, `terminal_policy=NONE` is rejected, and operation creation requires a tenant-owned ACTIVE payment terminal with effective `active_from` bound to the ACTIVE register. Direct register IDs cannot bypass tenant/status/kind validation. The two-Compose journey provisions that terminal through the public API and completes a MiniPOS CARD sale through both Caddy ingresses. Simulator/STUB still cannot prove approve/decline/timeout/reversal/STAN/RRN semantics of a real terminal SDK | vendor/acquirer adapter plus failure/reconciliation HIL |
| Per-command protocol semantics | PASS для всех Daisy/Datecs `Supported` и `Optional`: 88 Daisy/73 Datecs классифицированы; 45 Daisy core + 16 optional и 40 Datecs core + 12 optional имеют validated builders/parsers и golden gate. Daisy optional закрывает display/printer/drawer, invoice customer data, barcode 1–13, customer QR/template, display configuration, customer directory и model-conditional battery telemetry без повышения disposition. Datecs 135 остаётся diagnostics, регуляторное состояние берётся только из 71 | Daisy/Datecs `Privileged`/`Excluded` остаются за approved activation boundary; vendor/HIL proof для физических протоколов и EJ variants |
| BG-001…BG-025 executable trace | Машиночитаемый requirement→rule→API→test→evidence register и verifier реализованы; 7 PASS, 12 PARTIAL, 6 EXTERNAL_BLOCKED, 0 EXCLUDED_MVP. Все `PARTIAL` содержат только явно классифицированный внешний/HIL/legal evidence gap; `make supto-software-acceptance` запрещает скрывать программный разрыв этим статусом | vendor/HIL/legal строки допускают переход только по внешнему evidence |
| Signed release/provenance/vulnerability evidence | PASS для software gate: CycloneDX SBOM, exact-subject in-toto/SLSA statement, Ed25519 independent trust and optional fresh normalized scan are verified fail-closed; tamper, foreign SBOM and Critical/High findings are rejected | release-candidate GO still requires protected production key custody, an approved real scanner report, a fixed Metro `image-size` release for the current High build-time advisories, and external hardware/vendor/legal acceptance |
| OTA lifecycle | PASS для software policy: signed manifest/artifact, target/profile binding, monotonic anti-downgrade, restart-durable A/B health state, CAS and vulnerability-floor rollback/recovery are implemented. Physical secure boot, flash writer, recovery partition and vendor update compatibility are not software-simulated | approved hardware boot/update integration and destructive HIL |
| UI acceptance matrix | PASS для software Web interaction: a 42-case Android/iOS/Web source/build gate covers both apps. Production MiniPOS cashier does not download the employee directory or render management controls. BeeFiscal exposes editors only to ADMIN, audit reads to ADMIN/AUDITOR, report mutation/reconciliation to SUPERVISOR/ADMIN and read-only reporting to AUDITOR; unknown token roles fail closed. Its public collection filters omit an absent register and reject non-UUID values. Browser coverage includes readiness, diagnostics/capabilities, provisioning, BLE-session preparation, optional-terminal selection, immutable audit inspection, operations/reports/admin and the MiniPOS fiscal journeys. Physical assistive-tech, native interaction, real-IdP refresh and physical BLE evidence remain external | native Android/iOS interaction runners plus real-IdP and physical screen-reader/BLE/browser HIL |
| Long-running resilience | PASS для accelerated software model: executable 72h/5-minute network-flap and 7d/10-minute journal scenarios produce a machine-readable report and prove zero loss/duplicate commit, exact ambiguous ACK replay, daily restart, final cursor/hash, three-month retention and purge-chain continuity. Physical wall-clock SD/hardware endurance is not claimed | physical 72h/7d selected-hardware HIL remains deployment evidence, not a base software implementation gap |

## Внешние блокеры, формально исключённые из base MVP

Полный список и re-entry evidence находится в `MVP_GATES.md`. Daisy SMART SDK, Daisy Compact USB/electrical profile, DP-150 EUR firmware/electrical approval, BlueCash/BluePad/acquirer, microSD destructive power-loss, physical GATT/USB/UART HIL и НАП/БИМ/legal certification остаются `STUB_ONLY`, `DOCUMENTED_NOT_APPROVED`, `UNSUPPORTED` или `PROD_NO_GO`. Их нельзя закрыть simulator-тестом или конфигурационным флагом.

## Решение текущего среза

Актуальный evidence supersedes более ранние счётчики: unified regression 2026-08-14, exit 0; 74 effective canonical + 64 runtime OpenAPI operations, 138/138 generated contracts, 70 fault, 56 security, 42 UI и 25 BG trace cases. Исправленные BLE tenant/Edge/register/final-device identity, cross-language handshake context, signed-ticket downgrade guard, pre-scan advertising identity, deployment binding и полный SUPTO operator-security lifecycle прошли domain, Edge, TypeScript, OpenAPI, clean PostgreSQL/RLS, Android/iOS/Web, Smart/IoT и two-Compose/Caddy проверки.

- Software regression: **PASS**.
- Stage 24 repeatability: **PASS для текущего software slice** — два последовательных `make full-regression` на неизменённом состоянии завершились с exit 0, включая Ed25519 evidence/tamper gate и two-Compose E2E.
- Base functional software MVP: **PASS для simulator/STUB/unsupported capability profile**; выбранные external hardware/payment tracks остаются deployment gates и не повышаются simulator evidence.
- Stage 25 handover artifacts: **READY FOR HUMAN SIGNATURE** — versioned machine-readable acceptance record, release notes, operations/backup runbook, rollback plan and support/escalation guide are present and checked by `make handover-test`. The gate requires all Product/Engineering/QA/Security/Compliance signatures to remain `PENDING_EXTERNAL_SIGNATURE` until supplied by real approvers.
- Pilot: **NO-GO** без выбранного hardware track и HIL evidence.
- Production Bulgaria: **NO-GO** без vendor/legal/НАП/БИМ evidence, внешнего IdP deployment evidence и подписанного release pack. Runtime RLS software enforcement уже PASS и не считается оставшимся blocker.

Текущий software evidence: unified regression после SUPTO operator-security/export/trace remediation, 2026-08-14, exit 0. Surface: 74 effective canonical + 64 runtime OpenAPI operations, 138/138 generated contracts, 42 UI cases, 56 security cases и 25 BG trace rows. Product/employee/order/audit lists исполняют bounded opaque cursor contract, Fiscal collection reads сохраняют детерминированный total order. MiniPOS report исполняет обязательный `[from,to)` контракт, exact-cent split/reversal netting и сохраняет payment evidence через restart/backup/restore. Role boundaries fail closed, BLE-session preparation связывает ticket payload, REST tenant/Edge/register/final device, advertising identity и активную MiniPOS deployment configuration до radio scan, а MiniPOS принимает Fiscal только через точный public `/public/v1` base. Canonical JSON/CSV/XLSX доступны через tenant/artifact-bound download, Caddy E2E валидирует реальный XLSX. Это не повышает simulator/STUB до hardware, vendor, acquirer или legal PASS.
