services:
  fiscal-backend:
    extra_hosts: ["host.docker.internal:host-gateway"]
    environment:
      SMTP_HOST: host.docker.internal
      SMTP_PORT: "18025"
      SMTP_USER: ""
      SMTP_PASSWORD: ""
      SMTP_FROM: e2e-fiscal@beeloy.test
      SIMULATOR_CARD_TERMINAL_AVAILABLE: "true"
