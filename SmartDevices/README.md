# SmartDevices

Каталог Android-приложений для разных кассовых аппаратов.

## Структура
- `apps/` — отдельные приложения под разные модели/линейки устройств.
- `shared-sdk/` — общие компоненты (SDK, API-клиенты, бизнес-логика).
