# BeeFiscal / BeeMiniPOS software MVP release candidate

Release profile: `BG_MVP_FUNCTIONAL_NONPROD`  
Acceptance record: [`contracts/mvp-acceptance-v1.json`](contracts/mvp-acceptance-v1.json)

## Included

- Two isolated Fiscal and MiniPOS Compose projects, each with DEV/PROD overlays, Caddy and a separate PostgreSQL database.
- 92 OpenAPI operations with generated request/response runtime enforcement and typed clients.
- Fiscal sale, EUR totals, explicit cash/card split payments, append-only Fiscal and MiniPOS reversal, shifts, cash movements, reports, exports, immutable audit/outbox and reconciliation.
- MiniPOS touch checkout exposes exact ordered CASH+CARD split tender through `checkout-batch`. Its REST adapter consumes the enforced `MiniPosFiscalOperation`: only `FISCALIZED` succeeds, confirmed `FAILED` is final, and `UNKNOWN` remains frozen for GET-only reconciliation.
- The original fiscal receipt reference is persisted separately from the reversal reference in MiniPOS orders and checkout replay evidence, survives restart/backup/restore and is populated by both synchronous checkout and signed offline-sync webhooks. Fiscal operation/webhook envelopes use `fiscal_reference`; the public MiniPOS order uses the canonical OpenAPI field `receipt_reference`.
- The signed Fiscal webhook receiver uses a closed OpenAPI envelope for operation type, external/sale IDs, receipt reference and error evidence; undocumented fields fail before domain mutation.
- Canonical and additive runtime response schemas are composed fail-closed when they share a method/path/status. In particular, an order response that satisfies the canonical schema but omits runtime-owned `allowed_actions` is rejected before reaching the client.
- BLE authority issuance and refresh now revalidate the tenant-owned active operator in addition to the active register and final-device binding. Unknown, future or deactivated operators cannot obtain or extend local `fiscal.execute` authority.
- MiniPOS PROD bundles ignore both public MiniPOS and Fiscal static-token environment variables and use the personal OIDC access token for both public APIs. Browser E2E injects two forbidden token markers and scans the compiled artifact to prove neither secret is bundled; security regression now contains 32 executable cases.
- Registry-aware cash and card simulator flows. CARD requires an effective ACTIVE terminal binding and never falls back to cash.
- REST-issued BLE sessions, encrypted framing, replay protection, Edge SQLite journal, signed sync acknowledgements and three-month ACK-gated retention.
- BLE session lifecycle is bound to the issuing OIDC subject and a MiniPOS-prepared X25519 public key signed into the ticket; Edge HELLO verifies proof of possession and every established gateway channel rechecks expiry/revocation before frame decryption. MiniPOS removes stale READY state, cancels pending results and supports explicit re-issuance after expiry, failed connect, logout or configuration change. Refresh rotates nonce/fencing authority and revoke is a contract-checked idempotent empty `204`. Existing unbound session rows are deliberately invalidated and must be reissued.
- BeeMiniPOS and BeeFiscalApp Web/Android/iOS build surfaces; Daisy SMART S Android contract STUB.
- Daisy Compact protocol semantics now cover all 45 commands classified as `Supported`. Beyond the fiscal/readiness/EUR and sale/report set, commands 110/112/114/116/119/128/138/146/153 provide typed current-day payments, operator/refund totals, FM `P/F/E` queries, QR and issued-document/SHA1 evidence, 26 device constants, department sales/corrections and acknowledged text-report lines. Golden and negative C++ vectors remain software evidence, not USB/HIL approval.
- All 16 Daisy commands classified as `Optional` now also have typed semantics and golden/negative vectors: display/printer/drawer operations, invoice customer fields, barcode types 1–13, customer QR templates, display configuration, customer directory and Compact-family battery telemetry. Their disposition and model restrictions remain unchanged; this is not hardware activation evidence.
- The isolated two-Compose regression harness uses a separate PostgreSQL Unix-socket volume per stack and verifies the socket DSN before tests. This removes Docker Desktop DNS/VPN/bridge blackholes from backend-to-database communication while retaining separate database containers, internal `dbroute` networks and no PostgreSQL presence on shared ingress. DEV/PROD TCP topology is unchanged, and targeted two-Compose E2E passes.
- A unified `make full-regression` passes with exit code 0 for the final 45 core + 16 optional Daisy slice, including independent TCP PostgreSQL/RLS tests and the Unix-socket-isolated two-Compose cash/card/split/reversal/Z/UNKNOWN/restart/backup/restore journey.
- The mandatory regression gate now includes `go vet` for all three Go modules with isolated writable caches; the initial Fiscal, MiniPOS and Edge baseline is clean.
- Fiscal and MiniPOS PROD startup now rejects HTTP public routes, wildcard/non-origin CORS values and webhook keys shorter than 32 bytes. The PROD Compose profiles require the matching public/CORS variables, and `.env.example` was replaced with a complete DEV-only two-stack profile that is rendered by the regression gate.
- The unified regression passes with these guards enabled: three race/vet suites, 33 security cases, all contracts and platform builds, PostgreSQL/RLS, plus the complete two-Compose Caddy E2E all finished with exit code 0.
- MiniPOS HTTP connections now have explicit header/read/write/idle deadlines rather than Go's unbounded body/write defaults. The policy is executable security evidence, and two consecutive unchanged-tree full regressions passed with the resulting 34-case security matrix.
- Stage 1 governance is now machine-readable and regression-gated: two independent product boundaries, ten owned module domains, identifier policy and all 14 P0 decision records must remain complete, evidence-backed and production-blocked where external acceptance is pending.
- Fiscal Web CORS preflight now advertises the canonical webhook-endpoint DELETE operation. Approved origins can use the full OpenAPI mutation surface; foreign origins remain denied. A positive/negative preflight test raises the security matrix to 35 cases.
- BeeMiniPOS and BeeFiscalApp now bound every public-API fetch to four seconds, including the primary MiniPOS backend path. Hung connections abort deterministically and release timer/signal resources instead of freezing cashier/admin busy state; executable timeout and UI gates cover the policy.
- BeeFiscalApp no longer embeds or accepts a public static admin token in PROD. It now uses its own OIDC Authorization Code + PKCE client/scheme, blocks every API call before personal login and exposes logout; the real production Web bundle is scanned and exercised in Playwright.
- The post-OIDC unified regression passed end to end with 36 security cases and 25 UI acceptance cases, including both production bundle scans and pre-login fail-closed checks.
- MiniPOS and BeeFiscalApp now refresh expiring OIDC sessions in memory and fail closed to personal login when refresh authority is absent or rejected. Provider tokens are never persisted; deterministic expiry boundaries are covered by the 37-case security and 27-case UI matrices.
- The unified full regression passed after this lifecycle cutover, including all platform bundles and the two-stack Caddy/PostgreSQL recovery E2E.
- Malformed token lifetime metadata and production API `401` now invalidate UI authority immediately. MiniPOS also tears down/revokes its BLE session on personal-authority loss rather than leaving the local route READY.
- The post-hardening unified regression passed with all 29 UI acceptance cases and the complete two-stack recovery journey.
- Operator-owned open shifts are now recoverable through the public API after app/backend restart. Recovery is filtered by employee/register/state, denies another OIDC employee, clears stale cart context on auth loss and is covered by a real browser reload plus two-Compose restart assertion; UI acceptance contains 30 cases.
- The unified regression passed after the 91-operation recovery cutover, including PostgreSQL restart and both public Caddy routes.
- Blocked Z-report closure now has a dedicated server-authorized reconciliation path. It reads the original operation, cannot repeat Z when its ID is known, blocks all sales until final evidence and closes only with a final fiscal reference; security/UI gates are 38/31.
- The unified regression passed after the 92-operation Z-reconciliation cutover.
- CASHIER read access is now employee-scoped: employee directory and tenant-wide report are denied, order lists exclude other employees, and production UI neither fetches nor renders management data without supervisor/admin authority. Security/UI matrices are 39/32.
- The unified full regression passed after this least-privilege increment: all 92 request/response contracts, 39 security and 32 UI cases, platform exports, PostgreSQL/RLS and the independent two-Compose/Caddy recovery journey completed with exit code 0.
- Fiscal tenant-wide GET access and BeeFiscalApp controls are now role-specific. Cashiers cannot read workforce, audit, webhook, report/export or provisioning collections; ADMIN alone sees editors, AUDITOR receives report read-only UI, and report/reconciliation commands require SUPERVISOR or ADMIN. Opaque role claims fail closed. The post-fix full regression passed with 40 security and 33 UI cases.
- The native regression gate now builds BeeFiscalApp Android and iOS Expo artifacts as well as MiniPOS instead of inferring three-platform coverage from MiniPOS alone. It caught and rejected an incompatible ESM package-mode change before release evidence was updated.
- BeeFiscalApp now implements the missing Stage 17 device control plane: redacted diagnostics/capabilities, provisioning sessions, REST-issued proof-of-possession BLE session preparation, separate transport/final-device metrics and explicit optional POS-terminal configuration. These paths are exercised in the browser journey and the UI matrix is 34 cases.
- MiniPOS now enforces the public Fiscal boundary at startup in DEV and PROD: the base must be exact HTTP(S) `/public/v1` with no credentials/query/fragment or internal/database scheme. A mandatory architecture gate proves independent Compose services, DB mounts and private networks and scans for private imports/internal calls. Security coverage is 41 cases.
- Canonical JSON/CSV/XLSX exports are now downloadable through the public artifact route instead of producing an inaccessible manifest. Downloads enforce tenant and exact artifact-ID ownership, expose correct MIME/extensions, and canonical periods use strict `[from,to)` semantics to avoid adjacent-period duplication. Direct API coverage plus a real XLSX Caddy E2E raise security evidence to 42 cases.
- MiniPOS list behavior now matches OpenAPI: products, employees and orders honor opaque `cursor` plus bounded `limit`, return canonical page metadata and use deterministic ordering across requests/storage modes. Direct API tests cover two-page traversal, duplicate prevention and invalid limit/cursor rejection.
- Fiscal paginated collections now use a deterministic timestamp-plus-ID total order, including equal-timestamp tie-breaking for resources, operations, shifts and audit events. The public operation list test proves a complete two-page traversal in canonical order.
- Handover evidence now derives its contract and BG trace counts from the authoritative OpenAPI/generated artifacts. The acceptance record was corrected from the stale 90-operation snapshot to 73 canonical + 19 runtime and 92 request/response contracts; an executable negative test ensures future drift blocks handover.
- MiniPOS sales reporting now matches OpenAPI end to end: required RFC3339 `from/to`, strict half-open periods, exact EUR net gross and deterministic payment breakdown. Validated CASH/CARD type and amount evidence is durable in the new typed `payments` JSONB projection without sensitive card data; reversals net against the original tenders. Browser and two-Compose tests prove split/reversal totals survive backend restart and PostgreSQL backup/restore. UI acceptance is now 35 cases.
- Order responses do not expose the internal per-order tender evidence: the public single/list DTOs are sanitized centrally before OpenAPI enforcement, while the documented aggregate `payments[]` remains present only on the sales report. Regression tests prove this storage/API boundary in both directions.
- Fiscal shift listing now honors the documented `register_id` query filter before stable pagination.
- Both Expo applications now follow OpenAPI `next_cursor` links for all displayed collections. This prevents catalogs, active-shift recovery, reversal candidates and fiscal administration/history from being truncated after the first page; malformed cursor sequences stop with an explicit error rather than looping indefinitely.
- BeeFiscalApp adds the roadmap-required immutable audit journal screen. The tab is visible only to ADMIN/AUDITOR, uses the public paginated `audit-events` contract and displays actor, action, object, occurrence time, UNP and hash evidence; browser acceptance exercises the path. UI evidence now contains 37 cases.
- BeeFiscalApp removes the invalid hard-coded `FD000001` collection filter. Empty configuration now performs an unfiltered read, while supplied register filters plus probe/report/BLE/update/binding paths are UUID-validated before calling the public API. Invalid mutation controls fail closed. Unit/browser evidence raises the UI matrix to 38 cases.
- OIDC/OAuth software enforcement, FORCE RLS, release SBOM/provenance/signature verification and signed OTA policy.

## Compatibility disposition

- Daisy SMART S: `STUB_ONLY`, impossible to activate in PROD.
- Daisy Compact S 01 and DP-150 MX: protocol semantics documented and tested without claiming hardware approval.
- BlueCash-50 and BluePad-50 Plus: `UNSUPPORTED/EXCLUDED_FROM_PROD` until vendor/acquirer evidence exists.
- Currency: EUR for current operations; the periodized compliance export preserves the legal BGN/EUR boundary.

- MiniPOS register and Fiscal-device identities now follow the canonical UUID contract end to end. Legacy aliases such as `FD000001` are rejected by generated request and domain validation, while the UI blocks shift/BLE/readiness activity until valid deployment IDs are supplied. Targeted OpenAPI, Go, TypeScript and browser gates pass; UI acceptance is 39 cases.
- The corresponding full regression passed with all 92 request/response contracts, 21 fault, 42 security and 39 UI cases, both-app platform exports, PostgreSQL/RLS and isolated two-Compose/Caddy E2E.
- Cloud checkout now discovers the active end-device binding from the configured register over the canonical public Fiscal API. Production no longer requires a second hidden device-ID setting, and inactive, mismatched, unbound or malformed registry responses fail closed before checkout. Targeted unit/browser gates pass; UI acceptance is 40 cases.
- The public device-resolution cut passed the complete regression, including native/web bundles, generated contracts, PostgreSQL/RLS and two independent Compose/Caddy products.
- Shift recovery now fails closed when the autonomous workstation has no valid Fiscal register configuration. It cannot silently recover an employee shift from another register; a browser regression proves the broad query is never emitted. UI acceptance is 41 cases.
- The scoped-recovery cut passed the complete regression, including restart recovery through the two independent Caddy ingresses and autonomous PostgreSQL databases.
- MiniPOS now owns the duplicate-Z prevention invariant locally: concurrent close requests revalidate `OPEN` under lock before persisting `CLOSING`. A race test with the first Fiscal request held in-flight proves only one Z POST can escape. Fault coverage is 22 cases.
- The concurrent Z-close cut passed the complete regression with race detection, restart persistence and two-Compose/Caddy Z evidence.
- MiniPOS storno now has a durable pre-network freeze. Competing reversal requests with different keys cannot both reach Fiscal, and a crash/timeout cannot leave the original order apparently safe to reverse again. A race test proves exactly one storno call; fault coverage is 23 cases.
- The reversal-freeze cut passed the complete regression, including payment/report totals, restart/restore and the isolated two-Compose append-only storno journey.
- A signed storno webhook may now win the race with its HTTP response without confusing the POS: the same confirmed operation is treated as idempotent success. Race-tested fault coverage is 24 cases.
- The webhook-ordering cut passed the complete regression, including signed evidence, PostgreSQL recovery and two-Compose/Caddy reversal reporting.
- Fiscal sales now reserve payment durably before driver execution. Concurrent payments cannot both observe `OPEN` and issue two fiscal side effects; the canonical `PAYMENT_PENDING` state and `EXECUTING` operation are committed together. Fault coverage is 25 cases.
- The payment-reservation change passed the full unified regression, including the public two-Compose/Caddy transaction and restart/recovery journey.
- Completed sale, operation and receipt artifact now share one atomic durable commit. A post-device storage failure remains conservatively reserved and is recovered as `UNKNOWN/RECONCILE` on restart, with no blind fiscal replay or orphan receipt. Fault coverage is 26 cases.
- Atomic finalization and restart recovery passed the complete unified regression and isolated two-product recovery journey.
- Restart recovery now fail-closes every interrupted fiscal command (`FISCAL_SALE`, storno, report and cash-operation families) as `UNKNOWN/RECONCILE`, never as an automatic device retry. Fault coverage is 27 cases.
- Generalized interrupted-command recovery passed the full unified regression and two-Compose/Caddy restart journey.
- Direct Fiscal reversal is now CAS-reserved before driver dispatch, preventing concurrent double storno; X/Z/cash/device commands also require a durable `EXECUTING` record before side effects. Interrupted reversal recovery updates both sale and operation to `UNKNOWN/RECONCILE`, and successful storno uses canonical Fiscal Sale `CANCELLED`. Fault coverage is 30 cases.
- Durable storno and generic fiscal-command reservation passed the full unified regression and isolated two-product recovery journey.
- Fixed rollback of a failed first mutation against an empty database. Compliance export manifests, operations, all period artifacts and audit evidence are now atomically published, preventing orphan artifacts and incomplete completed exports. Fault coverage is 32 cases.
- Empty-snapshot rollback and atomic export publication passed the full unified regression, PostgreSQL/RLS integration and isolated BGN/EUR export recovery E2E.
- Report generation now atomically publishes the terminal fiscal operation, report resource, immutable artifact and audit evidence. FAILED/UNKNOWN device results publish no completed report; post-device storage failure recovers as UNKNOWN without replay. Fault coverage is 34 cases.
- Atomic report lifecycle passed the full unified regression, including Z-report API and two-product restart recovery.
- Terminal payment/reversal state and webhook outbox evidence now commit atomically with sale, operation and receipt. Removed the post-fiscalization HTTP outbox write that could return 500 after success; duplicate queue requests remain idempotent. Fault coverage is 35 cases.
- Atomic terminal state/outbox passed the full unified regression, webhook dispatcher suite and isolated payment/storno recovery journey.
- Added atomic webhook evidence for X/Z/cash/device-management commands and report completion. Successful reports publish both `fiscal.operation.updated` and `register.report.completed` in the same transaction as operation/report/artifact; failed publication exposes no event. Fault coverage is 37 cases.
- Generic-command and dual report webhook completeness passed the full unified regression, dispatcher suite and isolated Z-report recovery journey.
- Reconciliation start now atomically records `RECONCILING`, audit and a version-bound `fiscal.operation.reconciliation_required` event marked lookup-only. Storage failure leaves the operation UNKNOWN and publishes no event; no device command is replayed. Fault coverage is 38 cases.
- Atomic lookup-only reconciliation start passed the full unified regression, BeeFiscal allowed-actions UI, dispatcher and isolated recovery journey.
- Fixed cross-tenant/register shift-close denial of service. Operations now retain register identity; only unresolved EXECUTING/UNKNOWN/RECONCILING work on the same tenant/register blocks closure, with legacy sale fallback. Fault coverage is 40 and security coverage is 43 cases.
- Tenant/register-scoped shift isolation passed the full unified regression, typed PostgreSQL/RLS restart and isolated two-product shift recovery journey.
- OpenShift now atomically permits exactly one active/unresolved shift per tenant/register and cannot open over RECONCILIATION_REQUIRED. PostgreSQL migration adds operation register identity and upgrades the partial uniqueness index while preserving tenant autonomy. Fault coverage is 42 cases.
- Atomic OpenShift uniqueness and migration 006 passed the full unified regression, clean PostgreSQL/RLS initialization and isolated shift recovery journey.
- Fiscal sale identity no longer depends on the selected persistence adapter. Tenant-scoped external order IDs and issued UNPs are rejected before mutation, concurrent creates have one winner, and recovery from a stale UNP counter skips already-issued numbers without reuse. Fault coverage is 44 cases.
- The sale identity/UNP cut passed the full unified regression: 92/92 contracts, 44 fault, 43 security and 41 UI cases, Android/iOS/Web builds, clean PostgreSQL typed/RLS checks and isolated two-product Caddy recovery.
- Both public mutation APIs now use a durable cross-replica `PENDING` idempotency claim before any business/device side effect. Concurrent first requests have one executor; completed and ambiguous 5xx results are replayed exactly without re-execution, while a still-running claim returns retryable conflict. Typed PostgreSQL migrations Fiscal 007 and MiniPOS 010 persist the claim state. Fault coverage is 48 cases.
- The durable idempotency cut passed the full unified regression after preserving the expected UNKNOWN/outage 502 replay: 92/92 contracts, 48 fault, 43 security and 41 UI cases, clean PostgreSQL/RLS initialization, Android/iOS/Web, device/IoT suites and isolated two-product Caddy restart recovery.
- Orphan idempotency claims no longer remain indistinguishable from a live request after process restart. Both backends persist and replay an explicit `503 IDEMPOTENCY_OUTCOME_UNKNOWN` while retaining `PENDING`, so the key cannot be reclaimed for blind execution. Fault coverage is 50 cases.
- The orphan-recovery cut passed the full unified regression: 92/92 contracts, 50 fault, 43 security and 41 UI cases, clean PostgreSQL/RLS, all target builds and isolated Caddy outage/restart/backup recovery.
- Idempotency fingerprints now cover canonical query parameters, `If-Match`, `X-App-Instance-Id` and the request body with unambiguous length-prefixed framing in Fiscal and MiniPOS. The same key cannot replay a result produced under a different optimistic condition, query scope or application instance.
- The complete-request-fingerprint cut passed the full unified regression: 92/92 contracts, 52 fault, 43 security and 41 UI cases, clean PostgreSQL/RLS, all target builds and isolated Caddy outage/restart/backup recovery.
- Idempotent response replay now durably reproduces the original `Content-Type`, `ETag`, `Location`, `Retry-After` and `Cache-Control`. A narrow allow-list deliberately excludes `Set-Cookie` and arbitrary headers, preventing credentials or per-connection state from crossing requests. Fault coverage is 54 cases.
- The representation-header replay cut passed the full unified regression: 92/92 contracts, 54 fault, 43 security and 41 UI cases, clean PostgreSQL/RLS, all target builds and isolated Caddy outage/restart/backup recovery.
- Fiscal resource business keys are now enforced atomically and consistently in the repository and PostgreSQL: one organization per tenant and case-insensitive tenant-scoped code/serial uniqueness for locations, registers, operators and devices. Fault coverage is 55 cases.
- Fiscal administrative resources now enforce version CAS at the repository commit boundary. Detached map copies prevent pre-commit alias mutation, and concurrent requests carrying the same `If-Match` produce one committed version instead of a silent lost update. Fault coverage is 56 cases.
- The resource identity/CAS cut passed the full unified regression: 92/92 contracts, 56 fault, 43 security and 41 UI cases, clean PostgreSQL/RLS, all target builds and isolated Caddy outage/restart/backup recovery.
- Durable idempotency is now principal-bound in both products using verified OIDC issuer and subject, without storing bearer credentials. Cross-principal replay inside one tenant is rejected. Coverage is 58 fault and 45 security cases.
- RS256 OIDC verification now retains the canonical validated issuer in request claims for both products, fixing production MiniPOS employee identity lookup against real IdP tokens.
- The principal-bound replay/OIDC issuer cut passed the full unified regression: 92/92 contracts, 58 fault, 45 security and 41 UI cases, clean PostgreSQL/RLS, all target builds and isolated Caddy outage/restart/backup recovery.
- MiniPOS employee identity/session authority is now granted only after an atomic ACTIVE employee check; issuer normalization matches OIDC, and product/employee `active` consistently follows `status`. Migration 011 repairs legacy mutable projections and enforces these catalog/workforce rules in PostgreSQL. Coverage is 59 fault and 47 security cases.
- The identity-authority/catalog-consistency cut passed the full unified regression: 92/92 contracts, 59 fault, 47 security and 41 UI cases, clean PostgreSQL/RLS, all target builds and isolated Caddy outage/restart/backup recovery.
- Sale/order totals now use checked fixed-point arithmetic with canonical quantity scales and deterministic half-up cent rounding. Negative line prices and integer overflow are rejected before persistence or fiscal execution. Fault coverage is 61 cases.
- The checked-arithmetic cut passed the full unified regression: 92/92 contracts, 61 fault, 47 security and 41 UI cases, clean PostgreSQL/RLS, all target builds and isolated Caddy outage/restart/backup recovery.
- Added end-to-end absolute EUR line discounts: MiniPOS totals and checkout, effective OpenAPI-generated clients/validators, Fiscal sale and Edge offline materialization now preserve the same discount and reject negative, foreign-currency or over-gross values.
- Manager discount controls support percentage and exact EUR input on Android/iOS/Web; server-side RBAC prevents cashier API bypass. The slice passed full regression: 92/92 contracts, 64 fault, 48 security and 42 UI cases, clean PostgreSQL/RLS, target builds and isolated Caddy recovery E2E.
- Expanded compliance JSON/CSV/XLSX artifacts from identifier rows to full sale evidence: lines, discounts, tax groups, exact total, payments and fiscal receipt linkage. BGN/EUR period exports now reject stored monetary evidence in the wrong currency instead of silently relabeling it.
- Fiscal receipts now freeze the actual end-device ID, serial, FMIN/FM and version identity atomically before device execution. Later register rebinding cannot rewrite historical receipt/export evidence; PROD activation and typed PostgreSQL enforce the snapshot fields.
- Compliance exports now support OpenAPI-defined point-of-sale and fiscal-device filters against immutable sale-time snapshots. Location is frozen when the sale is created, device identity when payment execution is reserved, and PostgreSQL migration 011 provides an indexed historical projection.
- The historical-filter slice passed full regression: 92/92 OpenAPI contracts, 69 fault, 49 security and 42 UI cases, clean PostgreSQL/RLS, all target bundles/device suites and isolated two-product Caddy recovery E2E.
- Corrected BLE end-device routing: `edge_id` remains the advertising/runtime identity while `device_id` is the actual registry-bound fiscal apparatus. Signed authority and offline payloads now bind location/register/final device, and a later rebind invalidates refresh instead of silently retargeting the session.
- The corrected BLE identity slice passed full regression including signed-ticket downgrade rejection: 92/92 OpenAPI contracts, 70 fault, 51 security and 42 UI cases, clean PostgreSQL/RLS, all target bundles/device suites and isolated two-product Caddy recovery E2E.
- Closed a cross-language BLE handshake defect: MiniPOS and Edge now derive keys from the identical tenant/location/register/Edge/final-device/session/protocol context. Golden-vector and signed-ticket/envelope consistency tests raise the security matrix to 53 cases. The complete unified regression passed with 92/92 OpenAPI contracts, 70 fault and 42 UI cases, PostgreSQL/RLS, every target bundle/device suite and the isolated two-product Caddy recovery E2E.
- Web, Android and iOS BLE discovery now require the exact REST-authorized Edge advertising identity before opening GATT. Foreign peripherals advertising the same service can no longer capture bootstrap selection; the subsequent signed proof-of-possession handshake is unchanged. The complete unified regression passed with 48 MiniPOS tests, 54 security cases, all platform bundles and the isolated two-product recovery E2E.
- BLE discovery now evaluates ticket/REST Edge binding before scanning and requires the advertised identity to equal that Edge identity. The complete unified regression passed with 49 MiniPOS tests, 55 security cases, all target platform/device builds, PostgreSQL/RLS and isolated two-product recovery E2E.
- BLE session responses and generated OpenAPI clients now require the tenant identity carried by the signed ticket. MiniPOS rejects a token/deployment tenant mismatch before retaining BLE authority. Tenant keys are validated as non-empty OIDC tenant strings, not UUIDs. The complete unified regression passed across all contracts, platforms, persistence and two-product recovery E2E.
- MiniPOS now pre-resolves the ACTIVE register/final-device binding and rejects a BLE session returned for another tenant, register or device. It no longer overwrites signed response identity with local configuration. The complete unified regression passed with 50 MiniPOS tests, 56 security cases and all platform/persistence/recovery gates.

## Known limitations and NO-GO

This candidate is not a Bulgarian pilot or production release. Real hardware, vendor/acquirer, passwordless IdP deployment, vulnerability scan, protected signing key, NAP/BIM/legal and authorized-service evidence remain required. Simulator, STUB and accelerated soak results cannot close those gates.

## Verification

Run `make full-regression` and `make handover-test`. Release package creation and independent signature verification are documented in [`docs/release-evidence.md`](docs/release-evidence.md).

## Rollback

Use [`docs/rollback-plan.md`](docs/rollback-plan.md). Never roll back by editing or deleting a completed fiscal sale, operation, audit event or acknowledged Edge record.
